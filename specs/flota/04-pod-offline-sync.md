# 04 — POD, operación offline, outbox y sincronización

Fuente: §2 (EEVD y proyección sobre eventos, jamás contadores) · §3.E1 (POD, ítem 7) · §4.2 (regla de oro del motor de sync) · §4.6 (hechos, evidencia y lecturas) · §4.7 (outbox y undo, semántica cerrada) · §0 (constantes: idempotencia, undo 8 s, HTTP de sync, drift, capacidad) · §5.2 F4/F5 (secuencia canónica) · §5.3 (presupuesto de toques como contrato) · §5.7 (estados obligatorios y AA) · §7.4/§7.6 (append-only y terreno) · §9.2 (suite offline, covering array y k6) · §9.3 (centinelas 1, 4, 5 —solo el lado motor—, 6, 9, 10) — todas las citas resuelven contra `docs/PROMPT_MAESTRO_FLOTA.md`. Alimenta el hito §9.1.4(e) «POD offline (reuso nucleo-pod) + semáforo».

Alcance: **E1 solamente** (§3.E1). Chile solamente: es-CL, CLP entero, RUT (§0 Formatos). Los ganchos del §4.9 que tocan este módulo quedan en el estado EXACTO que fija su tabla (VIVA vs DDL-only según la fila), sin activar nada de E2/E3/E4 (detalle abajo).

## Propósito

Este módulo entrega dos cosas inseparables:

1. **El motor de sincronización del core, implementado UNA vez** (§4.2): outbox idempotente en el cliente + endpoint de sync de capturas en el servidor, usado por TODAS las clases CAPTURA de la plataforma (chequeos, lecturas, POD, devoluciones, cierres de recarga, respuestas de checklist, custodia — §4.2). Es la generalización de `packages/nucleo-pod` («outbox generalizado», §9.1) extraído en el hito 3; los demás módulos solo aportan tipos de captura, jamás su propio mecanismo de sync. Este módulo es dueño de la CONDUCTA del motor sobre el DDL del §4.6 que crea el módulo 00 (AC-FTEN-24) — 2xx siempre, flag + evento + fila en `review_queue`, ingesta en lote de `client_metric` y escritura de `evidence` —, jamás de su creación.
2. **El flujo POD de terreno (F4/F5 en su parte de captura)**: parada a parada, ≤4 toques, 100% offline, foto/GPS como mejoras progresivas, `dejado_en_punto` de primera clase, no-entrega = motivo + estado, undo 8 s (§3.E1.7, §5.2).

## Requisitos

### R1 — Contrato del motor de sync (regla de oro, §4.2)

- Dos clases de mutación, y solo dos, declaradas por `COMMENT ON TABLE` (PLANIFICACIÓN|CAPTURA) y exigidas por el linter de migraciones (§4.2). PLANIFICACIÓN valida online y rebota 422 con error tipado; las validaciones de dominio de ese lado pertenecen a los módulos de agenda/rutas/tarifas (centinela 5 §9.3), pero el MOTOR entregado aquí sí es dueño de imponer la frontera de clases: una mutación clasificada PLANIFICACIÓN que llegue por el camino de sync rebota 422 tipado con 0 filas — el 2xx-siempre del endpoint de sync rige SOLO para la clase CAPTURA (§0 HTTP; AC-FPOD-20).
- CAPTURA **jamás rebota al sincronizar**: el endpoint de sync responde 2xx SIEMPRE y degrada a **flag + evento + fila en `review_queue`** (§4.2, §0 HTTP). La validación bloqueante corre en el CLIENTE contra el snapshot (§4.2).
- Casos de degradación cableados (centinela 4, §9.3): SOC declarado >100 o fuera de perfil (la fila entra en `reading` que NO lleva CHECK de rango; la proyección `vehiculos.soc` queda clampada por trigger + flag, §0 SOC), odómetro menor (monotonicidad SUAVE, §4.6), lectura fuera de perfil, captura con módulo apagado y turno abierto (flag `modulo_apagado`, manda `turno.config_version_id`, §0 HTTP), captura post-revocación (§4.3). Rechazos = 0.
- Drift de reloj >5 min ⇒ flag, jamás rebote (§0); toda mutación viaja con doble reloj `event_time+tz_offset` y `record_time` (§4.6).
- En el servidor, los hechos aterrizan en `eventos` append-only con las columnas completas del §4.6 (UUIDv7, tipo catalogado, objeto, actor, dispositivo, event_time+tz_offset, record_time, payload, client_uuid, event_hash; `prev_hash` recién desde E3) con **secuencia monotónica por tenant como orden autoritativo**; el estado visible es proyección, jamás contadores mutables (§4.6, §2; AC-FPOD-21). El momento de cómputo de `event_hash` (¿al aterrizar en E1, o recién con la integridad etapa 2 del §3.E2?) queda como Pregunta al dueño #5.
- Segregación por tenant en todo el camino: `client_uuid` UUIDv7 + `UNIQUE(tenant_id, client_uuid)` + `ON CONFLICT DO NOTHING` (§0); colas, cache y logs segregados por tenant (§7.2); PK UUIDv7 generada en servidor (§0); el endpoint de sync queda cubierto por la suite HTTP A-contra-B autogenerada (recurso ajeno = 404, §0 · §9.2).
- Al aterrizar en `reading` rige la **idempotencia DOBLE** del §4.6: `client_uuid` Y (instrumento, sensor, ts_dispositivo) — ambas llaves con su ON CONFLICT son parte del contrato de aterrizaje de este motor. En E1 la segunda llave queda estructural pero VIVA y testeable: la tabla `instrument` es VIVA en E1 (§4.9), aunque sin seeds de frío y con `declarada` como única implementación de `ProveedorTelemetria` (AC-FPOD-04).

### R2 — Outbox del cliente (§4.7)

- Particionado por **(tenant, usuario)** y **JAMÁS se purga**: al autenticarse otra identidad en el mismo dispositivo se purga SOLO el snapshot (re-descargable); las capturas del usuario anterior persisten firmadas por el enrolamiento y se replayean aunque otro esté autenticado (§4.7; centinela 9).
- Secuencia monotónica por dispositivo; huecos ⇒ evento + Por revisar (evicción o manipulación), nunca pérdida silenciosa (§4.7).
- **Replay-on-startup y replay-on-online son el camino PRINCIPAL**; Background Sync es solo acelerador (Safari no la tiene, §4.7) y jamás dependencia (§7.6).
- El outbox alimenta la telemetría `client_metric` (profundidad, edad máx, evicciones de IndexedDB, persist() denegado, errores de sync, latencia — §4.6, §10), ingerida en lote por el MISMO endpoint de sync, 2xx siempre, `client_uuid UNIQUE` (§4.6). Las señales Datos/sync del semáforo (Anexo B) se derivan de estos datos y de eventos; las reglas `signal_rule` las siembra el módulo del semáforo.
- Precondición de plataforma (módulo de identidad): el enrolamiento no se completa sin display-mode standalone Y `persist()` concedido (§4.3), que es lo que protege este outbox de evicciones.

### R3 — Undo 8 s (única confirmación de capturas, §0 · §4.7)

- La captura se escribe INMEDIATAMENTE al outbox local con estado `pending_undo`; undo dentro de 8 s = mutación cancelada ANTES del replay (jamás sale del dispositivo).
- Si el replay ya ocurrió, undo = **supersede con motivo=`undo`**, EXCLUIDO por definición SQL del métrico de gaming (% PODs supersedidos, §4.7, §10).
- Cero modales para capturas: el undo de 8 s es la única confirmación (§7.6).

### R4 — Flujo POD de terreno (F4, §5.2; presupuesto §5.3 como contrato)

- Bucle lineal, UNA tarjeta por parada (qué + dónde + ventana + «7 de 23» en cifra 96px/700/tabular-nums, §0): «Llegué» (1) → «Entregado» (1) con undo 8 s y avance automático. **Entrega feliz = 2 acciones exactas** (§5.2 F4, §5.3).
- Convención de conteo cerrada (§5.3): un campo del teclado propio = 1 acción; el e2e cuenta ACCIONES/eventos, no keydowns; regresión que sube el conteo del camino feliz no se mergea.
- Variantes cerradas, jamás >4 acciones (§5.2 F4): **parcial** = stepper por ítem + `motivo_item` (qty_planificada/entregada/rechazada, §4.5); **no entregado** = motivo de catálogo por tenant + confirmar (3 acciones; motivo con `require_notes` según catálogo §4.5); **`dejado_en_punto`** de primera clase (3 acciones; encuadre fotográfico solo si bultos > `parametros.bultos_max_sin_receptor`, §4.4 · §5.2); evidencia extra SOLO si `stop_requirement` la exige — el flujo del operario SE ARMA POR DATOS, cero condicionales por vertical en la UI (§4.6).
- Estado de parada y resultado según máquina del §4.5 (`pending→…→done|cancelled`, resultado `exito|fallo|parcial`, `motivo_id`, `metodo_entrega`); los alias de UI «entregado/no entregado» jamás son nombres de esquema (§2).
- Bloque de recarga = una parada más: «Iniciar carga» 1 toque; al terminar SOC fin + confirmar (§5.2 F4); su cierre viaja por este mismo outbox (R6).
- Offline visible: «Entregada — por sincronizar» con contador REAL de cola (§5.2 F4, §5.7); las capturas JAMÁS muestran rechazo (§5.7).
- Banners de energía 30/20/15/10% no bloqueantes en la pantalla de parada (§5.2 F4); su lógica es del módulo EV — aquí solo se garantiza que no bloquean ni suman acciones.
- UI bajo sistema Miga y §0/§5.7: botón primario 56px full-width anclado abajo, targets ≥48px, teclas ≥64px, teclado numérico PROPIO siempre, dark mode automático, estados 4 obligatorios (AC-FPOD-22), contraste 7:1 en cifra operativa y semáforos, texto 200% sin truncar cifras, cero aria-labels vacíos (gate axe+Lighthouse, AC-FPOD-23), **VoiceOver completa el flujo POD — todas las variantes de F4 — como gate de CI** (§5.7; AC-FPOD-24), textos es-CL sin strings en inglés (§0 Formatos), selectores e2e solo por data-testid/term_key (§9.2).

### R5 — Evidencia e inmutabilidad (§4.5 · §4.6 · §7.4)

- `entregas_pod` **write-once con supersede** + `UNIQUE(encargo) WHERE cerrada AND supersede IS NULL` (§4.5). REVOKE UPDATE/DELETE en eventos, reading, evidence, firmas, POD (§7.4); corrección = supersede con motivo y autor.
- `evidence` es LA MISMA tabla para POD y verticales (§4.6); `sha256 NOT NULL` en binarios, write-once; **el sha256 viaja en la mutación ANTES del binario**; mismatch al re-hashear ⇒ flag, no rebote (§4.6).
- **Punto que el maestro deja genuinamente abierto y este módulo NO resuelve por su cuenta (Preguntas al dueño #4):** el numerador EEVD del §2 exige «≥1 fila válida en `evidence`» para la parada `entrega` con `estado='done'` y resultado `exito|parcial`, pero el enum de tipos del §4.6 no contiene ningún tipo capturable en la entrega feliz de 2 acciones sin foto ni firma (§5.2 F4). Ni la escritura de una fila `evidence` en el camino feliz ni la lectura del numerador contra `entregas_pod` se implementan hasta la respuesta del dueño; sin ella el módulo podría quedar correcto en sync y roto contra la variable norte (§2, §10).
- Foto y GPS: mejoras progresivas, JAMÁS dependencias (§3.E1.7, §7.6). Cámara denegada ⇒ degradación a captura sin foto + flag. GPS off por defecto (minimización §3.E1.15); GPS denegado se bloquea EN EL CLIENTE y se le dice al operario (§7.6) — la lectura adoptada (ver Preguntas al dueño #2) es que lo bloqueado es la captura de coordenadas, jamás el POD ni su sync; precisión mala jamás bloquea (§7.6).
- Minimización 21.719 en capturas: identificadores personales solo vía ID opaco de `personas`; cero PIN/RUT sin máscara en logs del sync (§3.E1.15, §7.8, scan de logs §9.2).

### R6 — Capturas no-POD por el mismo motor (outbox generalizado)

- Cierre de recarga: el chofer captura SOLO SOC/kWh; `costo_clp` queda NULL y lo completa el operador o un trigger de `tarifa_kwh_clp` — el flujo del chofer NUNCA contiene campos monetarios (§4.8; centinela 10). Replay offline ⇒ 2xx, cero rebotes.
- Chequeos pre/post (módulo 02, F3/F5), `devoluciones` (módulo 03: tabla y captura en la clasificación táctil del descuadre de F5, AC-FRUT-21), respuestas de checklist (módulo 02) y custodia (módulo 03, F2) usan este mismo contrato (§4.2): la TABLA y la PANTALLA son de esos módulos, el transporte y la degradación son de este. Este módulo no crea ni pantalla `devoluciones`.
- Capturas de dispositivo revocado (§4.3): ≤72 h ⇒ cuarentena con flag `post_revocacion`; >72 h ⇒ 2xx con flag `post_revocacion_tardia` + review_queue severidad alta + evento. La distinción es solo severidad de revisión — JAMÁS se descartan ni rebotan.

### R7 — Snapshot congelado por turno (§4.4 · §4.2 · §9.2)

- Toda la config viaja VERSIONADA y CONGELADA en el snapshot del turno (`turno.config_version_id`): un turno corre entero con UNA versión; cambios aplican al turno siguiente (§4.4).
- La validación bloqueante del cliente corre contra ese snapshot (§4.2); la suite offline de §9.2 incluye el caso «snapshot congelado».
- El snapshot se re-descarga tras purga por cambio de identidad (§4.7); p95 bootstrap <400 ms como umbral del pipeline de performance (§0 Capacidad, §9.2).

### R8 — Ganchos §4.9 que tocan este módulo: estado E1 EXACTO según la tabla (VIVA vs DDL-only), sin activar nada de E2/E3/E4

- Tipos de `evidence` `pin_destinatario` y `escaneo_codigo`: existen en el enum desde E1 como DDL; ningún seed E1 siembra `stop_requirements` de esos tipos (§5.2 F4, §3.E3); su UI/flujo es E3.
- `reading` es tabla **VIVA** en E1 (§4.9: «VIVA (tabla; sin seeds de frío)»); su enum `fuente` completo existe y en E1 la única implementación de `ProveedorTelemetria` es `declarada` (§4.9).
- `eventos.prev_hash` / cadena de hash: desde E3 (§4.6, centinela 8) — fuera de E1; en E1 el evento lleva los campos del §4.6 sin cadena.
- `instrument` es tabla **VIVA** en E1 (§4.9), referenciable desde `reading` (columna nullable, §4.6), sin seeds de frío; su rebote de planificación con feature ON pertenece a los módulos de planificación, no a este.

## Criterios de aceptación

- [x] (P1) F4 camino feliz: entrega = 2 acciones EXACTAS («Llegué» → «Entregado») con avance automático a la siguiente parada y undo 8 s como única confirmación (cero modales §7.6); e2e que CUENTA eventos de acción según la convención §5.3 (campo de teclado propio = 1 acción) y falla si el conteo sube — regresión bloqueante (§5.2 F4, §5.3) — oráculo: CI [AC-FPOD-01] — probado: el bucle vive en `dominio/pod-terreno.ts` (el avance es parte de la segunda acción, no un toque más) con 9 mutantes en `pod-terreno.test.ts`; `e2e/pod-feliz.spec.ts` cuenta 2 acciones exactas sobre la pantalla real, ve la tarjeta de la parada siguiente sin toque de avance, ejerce el undo dentro y fuera de la ventana de `UNDO.ventana_ms` con cero modales, y fija el baseline en `packages/metodo/panel/acciones-entrega-feliz.json`
- [x] (P1) Variantes cerradas sin superar 4 acciones (§5.2 F4), con e2e que cuenta acciones por variante según la convención §5.3: parcial = stepper por ítem + motivo_item (§4.5); no entregado = motivo de catálogo + confirmar (3 acciones); dejado_en_punto de primera clase (3 acciones; encuadre fotográfico SOLO si bultos > `parametros.bultos_max_sin_receptor` §4.4); evidencia extra solo si stop_requirement la exige (flujo armado por datos, cero condicionales por vertical §4.6). La degradación por cámara denegada y el gancho DDL-only van en AC-FPOD-17; el covering array, en AC-FPOD-18 (§9.2 «un AC por commit») — oráculo: CI [AC-FPOD-02]
- [x] (P1) 100% offline (§3.E1.7): con red cortada ANTES de llegar a la parada, el POD completo (todas las variantes) se captura y la UI muestra «Entregada — por sincronizar» con contador REAL de cola (§5.2 F4, §5.7); la captura JAMÁS muestra rechazo (§5.7); al volver la señal el replay vacía la cola sin intervención del operario. El bloqueo que se le anotó el 11-ago-2026 —«exige un `evento_tipo` nuevo y el catálogo se siembra solo por migración»— era un diagnóstico correcto con una conclusión equivocada: la migración prohibida al motor es `db/migraciones/` (KiloPan), no `db/migraciones-flota/`, que es suya. La sembró en `0049_captura_de_pod.sql` y el aterrizaje es real: la captura escribe en `eventos` —orden autoritativo del §4.6— con su doble reloj y su `client_uuid`, así que vaciar la cola es un hecho y no un borrado. Verde el 11-ago-2026: `e2e/pod-offline.spec.ts`, 3 de 3. El tercer caso es el que hace verdadero el «POD COMPLETO (todas las variantes)» del texto: con la red cortada antes de la primera parada cierra las cuatro salidas de F4 —feliz, parcial, no entregada y dejada en punto con el encuadre que exige el umbral del §4.4—, verifica en la BD que sin señal no aterrizó ninguna, y al volver la señal exige que cada una llegue con el `resultado` y el `metodo_entrega` de la máquina del §4.5. Sin esa aserción por variante, un `resultado` que el servidor no aceptara vaciaría la cola solo para el camino feliz y dejaría las otras tres girando sin que nadie lo note. El presupuesto de toques de cada variante NO se cuenta acá (es de AC-FPOD-02: dos baselines del mismo flujo se desincronizan), y el replay DOBLE es de AC-FPOD-04 — oráculo: CI [AC-FPOD-03]
- [ ] (P1) Idempotencia del outbox (§0): toda mutación de captura viaja con client_uuid UUIDv7 y el servidor la protege con `UNIQUE(tenant_id, client_uuid)` + `ON CONFLICT DO NOTHING`; captura con 0 señal y replay DOBLE (replay-on-startup + replay-on-online) ⇒ exactamente 1 fila (centinela 1 §9.3, métrica §2.6; `check.sh` corre replay doble en cada iteración §9.2); al aterrizar en `reading` rige además la idempotencia DOBLE del §4.6: replay de dos lecturas con `client_uuid` distinto y mismo (instrumento, sensor, ts_dispositivo) ⇒ exactamente 1 fila, probado con un instrumento sintético (tabla `instrument` VIVA en E1, §4.9) — oráculo: CI [AC-FPOD-04]
- [ ] (P1) Regla de oro en el endpoint de sync (§4.2): toda CAPTURA responde 2xx SIEMPRE y degrada a flag + evento + fila en review_queue — casos probados: SOC declarado >100 o fuera de perfil (fila en `reading` SIN CHECK de rango y `vehiculos.soc` clampado por trigger §0 SOC), odómetro menor (monotonicidad SUAVE §4.6), lectura fuera de perfil, drift de reloj >5 min ⇒ flag jamás rebote (§0) con doble reloj event_time+tz_offset/record_time en toda mutación (§4.6); rechazos = 0 (centinela 4 §9.3) — oráculo: CI [AC-FPOD-05]
- [ ] (P1) Módulo apagado con turno abierto (§0 HTTP, §5.5): la captura sincroniza 2xx con flag `modulo_apagado` + Por revisar y manda `turno.config_version_id`; el turno corre ENTERO con su config congelada y los cambios aplican al turno siguiente (§4.4); la suite offline cubre el caso «snapshot congelado» (§9.2) — oráculo: CI [AC-FPOD-06]
- [ ] (P1) Capturas offline de dispositivo revocado (§4.3): llegan ≤72 h ⇒ 2xx en cuarentena con flag `post_revocacion`; >72 h ⇒ 2xx con flag `post_revocacion_tardia` + review_queue severidad alta + evento; JAMÁS se descartan ni rebotan (la distinción es solo severidad de revisión; centinela 4 §9.3) — oráculo: CI [AC-FPOD-07]
- [ ] (P1) Undo 8 s con semántica cerrada (§0, §4.7): la captura se escribe INMEDIATAMENTE al outbox con estado `pending_undo`; undo dentro de 8 s = mutación cancelada ANTES del replay (jamás sale del dispositivo); captura + kill de la app a los 3 s ⇒ al reabrir la mutación existe y se replayea; undo post-replay = supersede con motivo=`undo` ⇒ 2 filas con la original intacta y EXCLUIDO por definición SQL del métrico de gaming (% PODs supersedidos §10) — oráculo: CI [AC-FPOD-08]
- [ ] (P1) Outbox particionado por (tenant, usuario) y JAMÁS purgado (§4.7): autenticarse otra identidad en el mismo dispositivo purga SOLO el snapshot (re-descargable); A captura 3 mutaciones offline → B se autentica → vuelve la red → las 3 filas de A existen (count=3), firmadas por el enrolamiento (centinela 9 §9.3) — oráculo: CI [AC-FPOD-09]
- [ ] (P1) Secuencia monotónica por dispositivo (§4.7): huecos ⇒ evento + Por revisar (evicción o manipulación), nunca pérdida silenciosa; replay-on-startup y replay-on-online como camino PRINCIPAL — la suite offline completa (§9.2) pasa con Background Sync deshabilitada (Safari no la tiene §4.7; jamás dependencia §7.6) — oráculo: CI [AC-FPOD-10]
- [ ] (P1) Inmutabilidad (pruebas SQL): el DDL de `entregas_pod` (§4.5) nace en ESTE módulo — no en el lote transversal del §4.6 que crea el módulo 00 (AC-FTEN-24) — con su migración, su `tenant_id` + CHECK constante y su `COMMENT ON TABLE` = CAPTURA (§4.2); write-once con `UNIQUE(encargo) WHERE cerrada AND supersede IS NULL` (§4.5); UPDATE/DELETE sobre POD/evidence/eventos como rol de app ⇒ 42501 y supersede ⇒ 2 filas con la original intacta (§7.4, centinela 6 §9.3). El contrato de transporte sha256/binario va en AC-FPOD-19 (§9.2 «un AC por commit») — oráculo: CI [AC-FPOD-11]
- [ ] (P1) Foto y GPS como mejoras progresivas, jamás dependencias (§3.E1.7, §7.6): cámara denegada ⇒ el POD se completa sin foto con flag; GPS off por defecto (§3.E1.15) y GPS denegado se bloquea EN EL CLIENTE con aviso visible al operario — el POD continúa sin coordenadas con flag y el sync jamás lo rebota; precisión mala JAMÁS bloquea; toda validación bloqueante corre en el CLIENTE contra el snapshot, nunca en el servidor de sync (§4.2) — oráculo: CI [AC-FPOD-12]
- [ ] (P1) Outbox generalizado (packages/nucleo-pod §9.1): el cierre de recarga viaja por el MISMO outbox como captura — «Iniciar carga» 1 toque y al terminar SOC fin + confirmar (§5.2 F4); chofer lo cierra offline capturando SOLO SOC/kWh, replay ⇒ 2xx, fila con `costo_clp` NULL (lo completa el operador o el trigger de `tarifa_kwh_clp`), cero rebotes y cero campos monetarios en el flujo del chofer (§4.8, centinela 10 §9.3) — oráculo: CI [AC-FPOD-13]
- [ ] (P2) `client_metric` ingerida en LOTE por el MISMO endpoint de sync, 2xx siempre, `client_uuid UNIQUE` (§4.6), con los tipos del enum (toques_flujo, eviccion_idb, persist_denegado, outbox_profundidad, outbox_edad_max, pwa_version, sync_error, latencia_ms); la telemetría de toques-hasta-completar por acción (`toques_flujo`) se emite desde el flujo F4 (§5.3). La cláusula «los paneles del §10 leen SOLO de client_metric y eventos» (§4.6) se verifica en el módulo dueño de esos paneles (05/§10; ver Dependencias), no aquí — oráculo: CI [AC-FPOD-14]
- [ ] (P2) Performance del sync en pipeline APARTE, jamás dentro del gate de 10 min (§9.2): k6 «ráfaga matinal» nightly/semanal parametrizado con la fila Capacidad COMPLETA del §0 (fuente única; el paréntesis del §9.2 la abrevia) — 2.000 dispositivos con turno abierto (N=2.000 bootstraps de snapshot), 5.000 usuarios concurrentes por célula y 100 replays/s sostenidos — con p95 bootstrap <400 ms y p95 sync <250 ms como umbrales que FALLAN el pipeline; dataset sintético con generador versionado y artefacto con tendencia — oráculo: CI [AC-FPOD-15]
- [ ] (P2) Conductor nuevo operando el flujo parada a parada <5 min sin ayuda (§5.3), validado en vivo con los pilotos; ítem de la checklist DONE-adopción con dueño humano nombrado: **Alexis** (dueño de DONE-adopción, §10); JAMÁS bloquea al loop (§9.2, §10) — oráculo: humano [AC-FPOD-16]
- [ ] (P1) Degradación de cámara y gancho DDL-only (desglosado de AC-FPOD-02): con cámara denegada, el encuadre fotográfico de `dejado_en_punto` (bultos > `parametros.bultos_max_sin_receptor` §4.4) degrada a captura sin foto + flag — jamás bloquea el flujo ni el sync (§7.6); y aserción sobre DDL/seeds: los tipos `pin_destinatario` y `escaneo_codigo` existen en el enum de `evidence` desde E1 y NINGÚN seed E1 siembra `stop_requirements` de esos tipos (§5.2 F4, §3.E3, §4.9) — oráculo: CI [AC-FPOD-17]
- [ ] (P1) E2e de la pantalla de parada bajo el covering array 2-way sobre TODOS los flags visibles al operario (archivo PICT versionado; agregar un flag sin regenerarlo ⇒ gate rojo), asertando en cada combinación el presupuesto de toques y el botón primario visible (§9.2; desglosado de AC-FPOD-02) — oráculo: CI [AC-FPOD-18]
- [ ] (P1) Contrato de transporte de binarios de `evidence` (§4.6; desglosado de AC-FPOD-11): `sha256 NOT NULL` en binarios; el sha256 viaja en la mutación ANTES del binario; mismatch al re-hashear en el servidor ⇒ flag, no rebote (la mutación sincroniza 2xx) — oráculo: CI [AC-FPOD-19]
- [ ] (P1) Frontera de clases impuesta por el MOTOR (§4.2): mutación sintética sobre una tabla clasificada `COMMENT ON TABLE` = PLANIFICACIÓN enviada por el camino de sync ⇒ 422 con error tipado y 0 filas (vehículo sugerido: el solape de agenda del centinela 5 §9.3); el 2xx-siempre del endpoint de sync rige SOLO para la clase CAPTURA (§0 HTTP) — oráculo: CI [AC-FPOD-20]
- [ ] (P1) Orden autoritativo y proyección (§4.6, §2): replay de dos capturas sobre el mismo objeto con `event_time` invertido (drift/offline) ⇒ ambas aterrizan 2xx, reciben secuencia monotónica por tenant creciente en el orden de llegada al servidor (orden autoritativo) y la proyección de estado visible refleja el orden del servidor; aserción adicional (test de esquema o grep) de que el estado visible deriva de `eventos` append-only y no existen contadores mutables — oráculo: CI [AC-FPOD-21]
- [ ] (P1) Los 4 estados obligatorios de la pantalla de parada (§5.7): vacío accionable · skeleton <50 ms y spinner solo >400 ms · error es-CL con recuperación (las capturas JAMÁS muestran rechazo) · sin conexión con contador real de cola — e2e que ejercita los cuatro — oráculo: CI [AC-FPOD-22]
- [ ] (P1) Gate AA de la pantalla de parada vía axe+Lighthouse (§5.7, §9.2): contraste 4.5:1 en texto, 3:1 en UI y 7:1 en la cifra operativa y semáforos; targets §0; foco visible; texto 200% sin truncar cifras; cero aria-labels vacíos — cualquier umbral incumplido deja el gate rojo — oráculo: CI [AC-FPOD-23]
- [ ] (P1) VoiceOver completa el flujo POD (§5.7): e2e con lector activado que completa TODAS las variantes de F4 (feliz, parcial, no entregado, dejado_en_punto, parada de recarga) de punta a punta; regresión bloqueante como parte del gate de CI — oráculo: CI [AC-FPOD-24]

## Dependencias

Numeración según el índice de specs del orquestador; el hito citado es el del §9.1.4 del maestro:

- **00 — Modelo de datos y tenancy — hito (a) (§4.1, §0):** BD por tenant con `tenant_id` + CHECK constante, `constants.ts` (undo 8 s, idempotencia, targets), linter de migraciones que exige el COMMENT PLANIFICACIÓN|CAPTURA (§4.2), y el DDL transversal del §4.6 donde este módulo escribe, CREADO por el módulo 00 en `tenant_template` con AC propio (AC-FTEN-24): `eventos`, `reading`, `evidence`, `client_metric` y `review_queue`. `entregas_pod` (§4.5) NO es de ese lote: la crea ESTE módulo (AC-FPOD-11). Sin el `UNIQUE(tenant_id, client_uuid)` no hay idempotencia.
- **01 — Identidad y enrolamiento — hito (b) (§4.3, §5.4):** dispositivo enrolado con secreto único, `is_standalone` + `persist()` concedido (condición de vida del outbox), revocación soft (origen de los flags `post_revocacion*`), firmas por PIN, lockout por usuario, y el centinela 12 (firma en dispositivo ajeno no desplaza sesión; los PODs posteriores que este módulo sincroniza deben salir válidos).
- **02 — Vehículos / energía / agenda — hito (c) (§3.E1.3/4/12, §4.5):** `turnos` con `config_version_id` (snapshot congelado), `reading` de odómetro/SOC y su proyección clampada, bloques de recarga cuyo cierre viaja por este outbox, banners de energía que la pantalla F4 muestra sin bloquear.
- **03 — Encargos / rutas / custodia — hito (d) (§3.E1.5/6, §4.5):** paradas, items, motivos por tenant, destinos, `stop_requirement` derivado del cargo_type (arma el flujo de evidencia), manifiestos y `devoluciones` (tabla y captura del módulo 03) cuyas capturas usan este motor.
- **05 — Semáforo «Hoy» — mismo hito (e) (§5.6, Anexo B):** consumidor co-dependiente: usa las señales del dominio Datos/sync (pendientes >30–60 min, sin sync >3–4 h, entrega sin evidencia tras sync, hueco de secuencia) que este módulo alimenta vía eventos y `client_metric`; las filas `signal_rule` las siembra el módulo del semáforo, no este. Es además el dueño del oráculo de la cláusula «los paneles del §10 leen SOLO de `client_metric` y eventos» (§4.6): se verifica en 05/§10, no aquí (AC-FPOD-14 queda acotado a lo verificable en este módulo).
- **Sistema Miga / packages — hito 0 y extracción (§5.1, §9.1, sin número de módulo):** tokens estructurales, teclado numérico propio, targets §0; `packages/nucleo-pod` extraído de KiloPan (hito 3) es la base que este módulo generaliza.

Aguas abajo (no son dependencias de este módulo, pero dependen de él): **06 — Tarifas/liquidación** (cada línea nace de exactamente UNA evidencia que este módulo sincroniza, §3.E1.9) y **07 — Portal del contratante** (muestra la evidencia por encargo, §3.E1.10).

## Preguntas al dueño

1. **Valor seed de `parametros.bultos_max_sin_receptor`:** el maestro define el parámetro (§4.4) y su uso — encuadre fotográfico en `dejado_en_punto` cuando bultos > umbral (§5.2 F4) — pero no fija el valor por defecto para los seeds de los tenants A/B/C (§10). ¿Qué umbral se siembra?
2. **Lectura de «GPS denegado bloquea en el cliente y lo dice» (§7.6):** conviven en el maestro «jamás depender de GPS para completar un flujo» (§7.6) y GPS como mejora progresiva (§3.E1.7). Esta spec adopta la única lectura compatible con ambas: lo bloqueado en el cliente es la captura de coordenadas (con aviso visible), jamás el cierre del POD ni su sync (AC-FPOD-12). Confirmar que NO existe ningún caso E1 donde GPS denegado deba impedir completar una parada.
3. **Binario de evidencia que nunca llega tras el 2xx de la mutación:** el sha256 viaja ANTES del binario y el mismatch ya degrada a flag (§4.6), y el Anexo B marca rojo «entrega sin evidencia tras sync». ¿Basta esa señal del semáforo o se quiere además un flag/plazo específico (p. ej. `binario_pendiente` en review_queue) para el binario nunca subido? No se implementa nada extra sin respuesta; rige solo lo escrito.
4. **Fila de `evidence` para la entrega del camino feliz (numerador EEVD, §2 · §4.6):** el §2 define el numerador EEVD como paradas `entrega` con `estado='done'`, resultado `exito|parcial` y «≥1 fila válida en `evidence`», y el §4.6 declara `evidence` LA MISMA tabla para POD y verticales — pero su enum {firma, foto, lectura, indicador_visual, archivo_logger, documento, pin_destinatario, escaneo_codigo} no contiene ningún tipo capturable en la entrega feliz de 2 acciones sin foto ni firma (§5.2 F4). Tal cual, toda entrega del camino feliz quedaría fuera del numerador (una ruta de pan de 20+ paradas sin fotos puntuaría 0) y el test de fixture de la EEVD del DONE-software (§10) se calcularía contra entregas invisibles. ¿La captura POD del camino feliz escribe una fila en `evidence` (¿de qué tipo del enum §4.6, o se agrega un tipo?) o el numerador del §2 debe leerse contra `entregas_pod` (como hace la liquidación §3.E1.9 con la clase `entrega_pod`)? Este módulo es el dueño de la escritura de `evidence` y NO implementa ninguna de las dos lecturas hasta la respuesta.
5. **Momento de cómputo de `eventos.event_hash`:** el §4.6 incluye `event_hash` en la definición canónica de `eventos` sin calificador de etapa (solo `prev_hash` se difiere «desde E3»), pero el §3.E2 dice «integridad etapa 2: `event_id` = hash SHA-256 canónico del evento». ¿`event_hash` se computa al aterrizar el evento ya en E1, o la columna queda DDL/nullable hasta la integridad de E2 (§3.E2)?
