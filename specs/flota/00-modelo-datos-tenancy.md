# 00 — Modelo de datos y tenancy física (BD por tenant, plano de control, dinero, ganchos)

Fuente: §0 (tabla canónica de constantes) · §4.1 (tenancy física: una BD por tenant, plano de control, runner de migraciones con canario) · §4.2 (regla de oro: clasificación PLANIFICACIÓN|CAPTURA en el DDL) · §4.4 (configuración por tenant) · §4.8 (dinero invisible en la BD) · §4.9 (ganchos de extensión: tabla DDL-only vs VIVA) — transversales que también gobiernan: §2 (métricas de plataforma 3, 4, 5 y 7), §3 (E1 ítems 1 y 14; selector de modo), §7.2/§7.5/§7.7/§7.8 (guardrails), §9.1(4)(a) (hito núcleo tenancy + constantes), §9.2 (gate y oráculos), §9.3 (centinelas 3, 10, 11, 13 y 14), §10 (DONE). Todas las citas resuelven contra `docs/PROMPT_MAESTRO_FLOTA.md`.

Módulo raíz de `apps/flota`. Entrega el hito §9.1(4)(a): núcleo tenancy (BD `control` + `tenant_template` + runner canario) + familia canónica de constantes + linter de migraciones + `docs/criterios-kiloruta.txt` congelado, con tag de build `[tenancy]` (§8: decisión fundacional irreversible de E1 — la escribe el modelo tope disponible).

**Qué NO especifica este módulo (vive en otro módulo o etapa):** el motor de sync/outbox y el comportamiento 2xx de capturas (hito e; aquí queda el DDL del §4.6, la clasificación DDL del §4.2 y su linter, jamás la conducta) · la CONDUCTA de `review_queue` (creación de filas por degradación y transiciones nueva→reconocida→resuelta: hitos e, módulos 04 y 05) · identidad, enrolamiento, personas/usuarios/dispositivos/firmas (§4.3, hito b) · tablas operativas del §4.5 (hitos c y d) · `energy_entry`(type fuel|charge, §4.6 — una de las tres tablas genéricas de aterrizaje del mecanismo §4.9), que nace en el hito c junto a su uso operativo de recargas · tarifas/liquidación/portal (hito f) · wizard, panel admin, pantalla «Funciones» y seeds A/B/C (hito g). De E2/E3/E4 no se construye NADA: solo queda el DDL de ganchos que la tabla del §4.9 marca, sin activarlos (p. ej. `prev_hash` de eventos es E3 y ni siquiera es de este módulo). La **instancia dedicada** del plan Empresa queda documentada, no construida (§0 fila Tenancy, §4.1).

---

## R1 — Familia canónica de constantes (§0)

- `constants.ts` es la fuente ÚNICA de la familia de constantes de plataforma para componentes Y tests; `constants.md` se GENERA desde ahí (§0 encabezado). Incluye, entre otras filas de la tabla §0: UUIDv7 server-side como PK, contrato de idempotencia (`client_uuid`), naming de tenancy (`t_<slug>`), enum FIJO de 6 roles (`admin_tenant · operador · chofer · responsable_carga · responsable_tecnico · cliente` — los packs no crean roles), dinero CLP `bigint` + `round_clp()`, unidades (Wh `int`, centésimas de °C `int`, décimas de % `int`; SOC `smallint` con CHECK 0–100 SOLO en la proyección `vehiculos.soc` — trigger que clampa + flag, del hito c —, y `reading.valor_int` SIN CHECK de rango: la captura fuera de rango entra con flag, §0 fila SOC y §4.2), códigos HTTP (404 recurso ajeno / 403 módulo apagado solo planificación-lectura / 2xx sync de captura), drift de reloj >5 min ⇒ flag, PIN (4 dígitos, argon2id, lockout 5 intentos POR USUARIO — jamás por dispositivo —, backoff server-side), fórmula EV única con factor_consumo 0,85 y override en `parametros`, límites de labels (≤12/≤24/≤40, caracteres prohibidos `# $ % ; < = >`), plazos legales (30 días pago, 8 días reclamo, 7 días disputa), capacidad k6 y formatos es-CL (`$12.500`, `dd-mm-aaaa`, RUT `12.345.678-5`) (§0).
- Grep-gate: un número mágico de esta familia hardcodeado fuera del archivo canónico ⇒ build rojo (§0).

## R2 — Tenancy física: una BD por tenant (§4.1)

- **Plantilla:** `tenant_template` es un artefacto versionado del repo y la 4ª vida de todo cambio de esquema; el alta de tenant crea `t_<slug>` vía `CREATE DATABASE … TEMPLATE` en segundos, dentro del wizard (§4.1). `tenant_info` (fila única: id, slug) queda sembrada en la provisión (§4.4).
- **Credenciales por tenant:** rol `app_t_<slug>` NOSUPERUSER, NOBYPASSRLS, sin ownership, con `CONNECT` SOLO a su BD; un bug de routing no puede cruzar datos porque la conexión de A físicamente no alcanza la BD de B (§4.1). Rol `migrator` separado para migraciones (§4.1).
- **Plano de control:** BD `control`, sin datos operativos de dominio: registro de tenants (slug→BD, plan, `modo`, estado), entitlements/billing SaaS — componente que en E1 se LIMITA al registro ya especificado: `plan_id` en `control.tenants` + las 3 tablas de entitlements, que viven en `control` por respuesta del dueño del 09-ago-2026 a la Pregunta 3; toda estructura o lógica de cobro SaaS (Stripe, página pública de precios) es E2 (§3 FUERA de E1), NO se construye ni queda como DDL: billing no figura en la tabla de ganchos §4.9 —, invitaciones de tenant nuevo (distintas de las invitaciones de enrolamiento del §4.3), grants de soporte, y agregados técnicos empujados por el job exportador (salud, adopción, EEVD agregada, backlog de sync). El payload del exportador tiene schema fijo TESTEADO: cero columnas de dinero/tarifas/clientes; la vista cross-tenant de e-auto lee SOLO de `control` (§4.1, §5.6).
- **Ruteo:** subdominio → lookup en `control` → pool de conexiones de SU BD (PgBouncer multi-database), con límite de pool POR tenant para que uno no agote el cluster (§4.1).
- **Dentro de cada BD tenant:** `tenant_id uuid NOT NULL` en TODA tabla de dominio con `CHECK (tenant_id = (SELECT id FROM tenant_info))` (la constante de la BD, §0), FKs compuestas `(tenant_id, id)` y `client_uuid` único — deja abiertas consolidación y extracción futuras sin migrar dominio (§4.1). RLS queda reservada a políticas DE ROL (§4.8 dinero; rol `cliente` confinado a su `empresa_cliente_id` — la política concreta del rol cliente se aplica sobre tablas del hito f/d). `set_config('app.current_role', $2, true)` por transacción — SET LOCAL siempre, jamás SET de sesión (§4.1, §7.2).
- **Migraciones ×N como código:** runner que itera todas las BD tenant — primero el canario sintético `t_canary`, luego el resto; `schema_migrations` por BD; el deploy NO se declara verde con alguna BD rezagada (§4.1, §9.3.13); en `scripts/deploy.sh` el paso de migraciones aborta ANTES de sustituir la versión servida (§9.1).
- **Prohibido (§4.1, §7.2):** consultas cross-database en runtime del producto (agregación SOLO vía exportador a `control`); superuser/BYPASSRLS/ownership en la conexión de la app; cache keys, colas, jobs, logs y backups sin segregar por tenant; tablas/columnas por vertical.
- **Offboarding (§2 métrica 7):** entrega al tenant de SU base de datos completa (`pg_dump` de su BD) — portabilidad Ley 21.719.

## R3 — Regla de oro en el DDL (§4.2)

- Toda tabla nueva se clasifica de forma explícita con `COMMENT ON TABLE` = `PLANIFICACIÓN` o `CAPTURA`; el linter de migraciones EXIGE la clasificación (§4.2). El comportamiento (422 en planificación, 2xx + flag + review_queue en captura) lo implementa el motor de sync UNA vez en el core (hito e) — este módulo fija el contrato en el esquema y el gate.
- El linter de esquema contra catálogos exige además, por tabla de dominio: `tenant_id` + CHECK de constante + índice + FK compuesta (§9.2).
- Este módulo entrega además el GENERADOR de la suite HTTP A-contra-B autogenerada del manifiesto de rutas (§9.2, centinela 2): infraestructura de gate del hito (a), consumida por todos los módulos 01–08, que solo la declaran en Dependencias. Entregable: AC-FTEN-26.

## R4 — Configuración por tenant (§4.4)

- **`control.tenants`**(slug=subdominio, bd, plan_id, `modo mi_flota|daas`, estado). Los modos son presets del MISMO sistema de entitlements, no código distinto, con mapeo CERRADO en §3 que este módulo entrega como filas/regla de resolución: preset `mi_flota` ⇒ tarifas, liquidación por cliente, portal del contratante y facturación OFF y ocultos (queda lo operativo puro); `daas` ⇒ el set del plan sin ese recorte; conmutar es aditivo, jamás destructivo (§3 selector de modo; la empresa_cliente implícita por trigger pertenece al módulo que crea `empresas_cliente`, hito d/f).
- **`tenant_theme`**(logo_url, accent_color RECHAZADO si <4.5:1, extras) (§4.4, §5.1 — el tenant personaliza EXACTAMENTE tres cosas como filas).
- **`tenant_terminology`**(term_key, singular, plural — ambos obligatorios): CHECK de largo POR TIPO de term_key (navegación ≤12, títulos ≤24, descripciones ≤40), caracteres prohibidos `# $ % ; < = >`, términos de sistema/auditoría excluidos por CHECK (§0, §4.4).
- **Entitlements, 3 tablas:** `features`(lookup_key, module) · `plan_features` · `tenant_feature_overrides`(enabled, motivo); entitlement efectivo = `override ?? plan`; límites cuantitativos como COLUMNAS del plan (§4.4; ejemplos de límites en Anexo A: 1 vehículo / 300 entregas-mes del plan Partida). Quién opera los toggles y su UI («Funciones») es del hito g (§5.5); este módulo entrega tablas y regla de resolución.
- **`vertical_template`**(vertical, terminologia, motivos[], checklists[], cargo_types[], config_ev, meta_eevd) — la meta EEVD vive como COLUMNA por vertical, jamás en prosa (§2, §4.4).
- **`grupos`**(padre_id): árbol ÚNICO de visibilidad, ortogonal al rol (§4.4). El DDL y el test de no-ciclos van en AC-FTEN-12; la APLICACIÓN de la visibilidad por grupos a alguna superficie es obligación E1 del §3.E1.1 (ítem 1 de la lista cerrada) que ninguna spec cubría: queda reclamada aquí en AC-FTEN-27, BLOQUEADA por la Pregunta al dueño 12 — el maestro no dice qué entidades se adscriben a un grupo ni qué superficies filtra, y esta spec no lo inventa.
- **`parametros`** por tenant: reserva_pct, factores (incl. factor_consumo con default 0,85 §0), umbrales, tarifa_kwh_clp, precio_diesel_litro_clp, bultos_max_sin_receptor, … (lista abierta en el maestro — ver Preguntas) (§4.4).
- **Versionado congelado:** TODA la config viaja versionada y congelada en el snapshot del turno (`turno.config_version_id`): un turno corre entero con UNA versión; cambios aplican al turno siguiente (§4.4). La tabla `turnos` es del hito c; este módulo entrega el mecanismo de versión referenciable.

## R5 — Dinero (§0, §4.8, §7.5)

- Todo monto es CLP `bigint`, entero en unidad menor; `round_clp()` es la función única de redondeo (§0). El devengo con UNA fuente SECURITY DEFINER es del módulo de liquidación (hito f) (§7.5).
- Tablas con montos (tarifas, liquidaciones, líneas, facturas, pagos, costos de energía — nazcan en el módulo que nazcan) llevan política RLS adicional `AS RESTRICTIVE` declarada `FOR SELECT` únicamente, que exige `app.current_role NOT IN ('chofer','responsable_carga')` — FOR SELECT y no total porque una RESTRICTIVE total rebotaría el INSERT del cierre de recarga del chofer (§4.8). El flujo del chofer jamás contiene campos monetarios: `costo_clp` lo completa el operador o un trigger desde `parametros.tarifa_kwh_clp` (§4.8).
- Este módulo entrega el patrón EJECUTABLE: política + test pgTAP con el rol de app real sobre una tabla FIXTURE de montos del harness de test (ninguna tabla de montos del §4.8 nace en este módulo; el TIPADO sí se ejercita ya con las columnas `*_clp` reales de `parametros`) — ver AC-FTEN-09/AC-FTEN-21; el centinela 10 completo (cierre de recarga offline+replay ⇒ 2xx, costo NULL) se cierra con los hitos c/e (§9.3.10).

## R6 — Ganchos de extensión por vertical (§4.9)

- Mecanismo: registro de atributos tipado — `attribute_definition` VERSIONADA + columna `attrs jsonb` validada por TRIGGER contra la definición vigente. NI tablas satélite por vertical NI jsonb libre; series y datos agregables SIEMPRE a tablas genéricas (`reading`, `energy_entry` — nace en el hito c, ver exclusiones —, `eventos`), jamás a jsonb. Un vertical nuevo = INSERT de filas, cero migraciones (§4.9, §2 métrica 4, §7.2).
- Estado E1 por gancho (tabla §4.9, literal):
  - `cargo_type` (raíz de extensión): **VIVA**.
  - `attribute_definition` + attrs validado: **VIVA**.
  - `stop_requirement`(parada, tipo_evidencia, obligatorio, orden — §4.6): **VIVA**; su FK a paradas se completa cuando el hito d cree `paradas`; ningún seed E1 siembra `pin_destinatario` (§5.2 F4).
  - `reading` para condiciones (temp/hum): **VIVA** (tabla genérica §4.6; SIN seeds de frío). Nace APPEND-ONLY (REVOKE UPDATE/DELETE al rol de app + trigger — §4.6 encabezado, §7.4) y con `valor_int` SIN CHECK de rango: el CHECK 0–100 vive SOLO en la proyección `vehiculos.soc` (trigger del hito c que clampa + flag; §0 fila SOC, §9.3.4).
  - `thermal_profile` / `alarm_rule`: **DDL-only**, con CHECK VIVO de la matriz de honestidad: prohibido activar `alarm_rule` `cumulative` o feature de compliance a tenant cuya única fuente sea `declarada` (§4.9, §7.7).
  - `excursion`: trigger VIVO (derivada, recalculable ante backfill) pero INERTE sin alarm_rules sembradas (§4.9).
  - `disposition` + auto-release: **DDL-only** (UI en E3) (§4.9); nace APPEND-ONLY (REVOKE UPDATE/DELETE al rol de app + trigger — §4.6 encabezado, §7.4).
  - `instrument` / `vehicle_certification`: tablas **VIVAS**; el rebote de planificación por certificación vencida se activa solo con feature ON y pertenece a los módulos de planificación (§4.9, §9.3.5).
  - `lot`(tlc, vencimiento) / `reference_document`(DTE 33/39/52/61, folio, emisor, `UNIQUE(tipo, folio, emisor)`): columnas/tablas nullables **VIVAS** desde el día 1 (§4.6, §4.9). La app JAMÁS emite DTE ni genera nada con apariencia de DTE: aquí solo se REFERENCIAN documentos (§7.3). Único creador: este módulo. `items.lote_id` (módulo 03) es FK hacia `lot`; los `reference_document` del DTE del manifiesto (módulo 03, F2) y del folio de la liquidación (módulo 06) son FILAS sobre esta tabla, jamás DDL propio.
  - `ProveedorTelemetria`: interfaz con ÚNICA implementación `declarada` (§4.9).

## R7 — Criterios KiloRuta congelados (encabezado del maestro, §9.1(4)(a))

- Primer ítem del hito (a): extraer UNA vez los criterios de la spec de KiloRuta a `docs/criterios-kiloruta.txt` con IDs cerrados `KR-01…KR-NN` y N total explícito; Alexis aprueba la lista. `docs/matriz-kiloruta.md` es tabla MD `ID | tabla/constraint | test (ruta::nombre)`. El gate verifica tres cosas mecánicas: count(filas)==N, cada ID exactamente una vez, cada test referenciado existe en el repo (encabezado «HECHO VERIFICADO»).

## R8 — DDL transversal de hechos, evidencia y telemetría (§4.6, §3.E1.14)

- El DDL de `eventos`, `evidence`, `audit_trail`, `client_metric` y `review_queue` nace en `tenant_template` al cierre del hito (a), porque los hitos (b) identidad, (c) vehículos y (d) encargos/custodia ESCRIBEN en esas tablas y se construyen ANTES del hito (e) (§4.1: la plantilla es la 4ª vida de todo cambio de esquema; §9.1(4)).
- Este módulo entrega: esquema, append-only (REVOKE UPDATE/DELETE + trigger donde §7.4 lo exige), enums cerrados y clase `COMMENT ON TABLE` (§4.2).
- Este módulo NO entrega ninguna conducta: el motor de sync (2xx siempre, flag + evento + fila en `review_queue`), la ingesta en lote de `client_metric` y la escritura de `evidence` del POD son del módulo 04 (hito e); las transiciones de `review_queue` (nueva→reconocida→resuelta) y su proyección semafórica son del módulo 05.

## Transversales que este módulo debe honrar en sus propios artefactos

- Cero datos personales reales en seeds/fixtures; RUTs sintácticamente válidos pero irreales; RUT enmascarado y PIN jamás en logs (§7.8). Cadenas centinela únicas por tenant en las BDs de prueba (§9.3.2, §10).
- Textos de error y docs generados en es-CL; grep de cero strings visibles en inglés (§0 Formatos).
- `guardrail.sh`: `DATABASE_URL` solo localhost en desarrollo; secretos solo en `.env.local` gitignored; grep bloqueante `TODO|FIXME|PLACEHOLDER|not implemented|lorem ipsum` en `src/` (§7.1).
- **Runbook de brechas (§7.8, obligación E1 que el maestro no asigna a módulo):** lo asume ESTE módulo, dueño del plano de control y de los runbooks operativos de plataforma (backups por BD tenant, AC-FTEN-16). Entregable: AC-FTEN-25.

## Criterios de aceptación

- [x] (P1) `packages/nucleo-comun/src/constants.ts` existe como fuente única de la familia §0 (con los formatos es-CL `$12.500`, `dd-mm-aaaa`, RUT `12.345.678-5` y el enum fijo de 6 roles) y `constants.md` se genera desde ahí; caso de rebote: un número mágico de la familia hardcodeado fuera del archivo canónico hace fallar el grep-gate (build rojo). Evidencia: `db/flota/gate-constantes.mjs` corre en `db/flota/gate.sh` y verifica DOS cosas —que `constants.md` refleje el `.ts`, y que ninguna de las `CIFRAS_VIGILADAS` (lista cerrada declarada DENTRO del archivo canónico, para que agregar una constante sin su vigilancia se vea en el mismo diff) aparezca duplicada en el código de FLOTA—; 5 mutantes en `db/flota/gate-constantes.test.mjs` (incluido «cada patrón dispara de verdad contra su propio valor», que evita una vigilancia decorativa, y «lista vacía ⇒ rojo») más 16 pruebas de coherencia de la familia en `packages/nucleo-comun/src/constants.test.ts`, que a propósito NO repiten los valores. Alcance DECLARADO por el gate en cada corrida: `packages/miga` queda fuera hasta AC-FMIG-01, que es quien hace derivar sus tokens de esta familia — oráculo: CI [AC-FTEN-01]
- [x] (P1) `tenant_template` versionada en el repo provisiona una BD tenant completa vía `CREATE DATABASE t_<slug> TEMPLATE …` con `tenant_info` (fila única id+slug) sembrada, y `check.sh --full` provisiona 2 BDs tenant desde la plantilla + runner + seed en cada corrida; caso de rebote: migración aplicada a tenants pero no a la plantilla deja la provisión de tenant nuevo desactualizada y el gate del runner lo detecta (exit ≠ 0). Evidencia: `db/flota/provisionar.mjs` (`plantilla | tenant <slug> | auditar`) construye la plantilla desde `db/migraciones-flota/tenant/`, crea `t_<slug>` por TEMPLATE y siembra la identidad —`tenant_info` con uuid generado por el SERVIDOR (`uuidv7()`, §0) y `tenant_actual()` reemplazada con ese uuid horneado como literal, sin cerrar la provisión hasta que `tenant_coherente()` sea verdadero: mientras la constante y la fila no coincidan, cada CHECK de `tenant_id` estaría validando contra otro tenant—; `db/flota/gate.sh --full` levanta el cluster y corre `db/flota/suite-bd/provisionar.test.mjs`, que en CADA corrida provisiona `t_gate_a` y `t_gate_b` desde la plantilla y los deja vivos como evidencia inspeccionable. 6 pruebas contra el cluster real (plantilla al día y SIN identidad sembrada —la plantilla no es un tenant—, dos tenants con ids distintos y de versión 7, segunda fila en `tenant_info` rebotada por la BD, alta sin `--recrear` que no pisa una base existente, y el mensaje propio cuando queda una conexión abierta contra la plantilla, que PostgreSQL solo reporta como «being accessed by other users») más 8 mutantes de la regla de rezago en `db/flota/provisionar.test.mjs`, que corren SIN cluster en cada iteración. El caso de rebote se ejerce como cadena verde→rojo→verde: se aplica una migración a `t_gate_rezagado` y no a la plantilla, `provisionar.mjs auditar` sale ≠ 0, y al quitar esa base vuelve a 0 —sin la mitad verde previa, el rojo no probaría nada—. ALCANCE DECLARADO: «runner» aquí es la aplicación de migraciones sobre la plantilla y sus tenants (`db/flota/aplicar.mjs`, una transacción y un sha256 por migración); el runner ×N con canario `t_canary` y rol `migrator` es AC-FTEN-07 y el centinela 13 se cierra allá. «Seed» es la siembra de identidad que este mismo AC nombra; el seed realista de los tenants A/B/C (§10) es del hito (g). CORREGIDO el 09-ago-2026, encontrado por la suite pgTAP de AC-FTEN-08 y no por esta: un alta que fallaba DESPUÉS de crear la base la dejaba en pie, y una base creada y sin sembrar es peor que ninguna —parece provisionada, `tenant_actual()` devuelve el centinela de la plantilla y todo lo que se escriba ahí queda atado a un tenant que no existe—; `t_canary` estuvo exactamente así. Ahora la provisión se deshace sola si la siembra falla, con su prueba (se siembra una identidad en la plantilla para que el INSERT de la copia choque contra el «una sola fila, para siempre», y se verifica que la base no quedó viva) — oráculo: CI [AC-FTEN-02]
- [x] (P1) Cada tenant tiene rol propio `app_t_<slug>` NOSUPERUSER, NOBYPASSRLS, sin ownership y con CONNECT solo a su BD, auditado por test de privilegios en CI; caso de rebote: autenticarse con credenciales de app del tenant A contra la BD de B es rechazado por Postgres (centinela 3). Evidencia: `db/flota/rol-app.mjs`, invocado por la provisión en el MISMO acto que crea la base — una BD de tenant sin su rol sería una base a la que solo llega el migrador, y el primero que la necesitara la abriría con el rol equivocado. Tres mecanismos, no uno: (1) atributos LOGIN NOSUPERUSER NOCREATEDB NOCREATEROLE NOBYPASSRLS; (2) `CONNECT` revocado a PUBLIC en TODAS las bases del cluster y otorgado explícitamente solo a `t_<slug>` — Postgres se lo regala a PUBLIC en cada base que nace, así que acotar únicamente la propia dejaría al rol de A entrando a la de B, a la plantilla y a `postgres`, que es exactamente el cruce que el centinela niega; el dueño (`migrator`) y el superusuario conservan acceso porque sus privilegios no salen de PUBLIC; (3) cero ownership, auditado con `pg_shdepend deptype='o'` (y no solo `pg_class`, que dejaría pasar una función propia del rol), con los permisos de datos otorgados por `ALTER DEFAULT PRIVILEGES FOR ROLE migrator` para que la tabla que el runner cree mañana no nazca invisible y el atajo sea regalarle ownership. El `pg_hba` del cluster pasó a `scram-sha-256` (`db/flota/cluster.sh`, bloque antepuesto e idempotente, con la línea `local` incluida para que el socket unix no sea puerta de atrás): las claves se generan al provisionar, se devuelven a quien llama y NO se escriben en disco (dónde viven en producción es del registro de `control`, AC-FTEN-04, y del §7.1). DECLARADO: `flota_admin` y `migrator` siguen en `trust` desde 127.0.0.1 — son los roles de operación del cluster de desarrollo, y cargarles un secreto a los scripts del repo es justo lo que el §7.1 no quiere ver escrito. 7 pruebas contra el cluster real en `db/flota/suite-bd/rol-app.test.mjs` (atributos + `posee == 0` · `alcanza` es EXACTAMENTE su base · la mitad positiva —con su clave entra a su base y lee su `tenant_info`, sin la cual todo lo demás sería verde por un rol que no funciona— · centinela 3 en AMBOS sentidos con `42501` y el nombre de la base ajena en el mensaje · clave equivocada ⇒ `28P01`, prueba que MUERE si alguien devuelve el `pg_hba` a `trust` · `CREATE TABLE`/`DROP TABLE` como la app ⇒ `42501` · tabla creada después por el migrador queda legible y escribible sin ownership) más 9 mutantes en `db/flota/rol-app.test.mjs` sobre la clave interpolada —`CREATE ROLE … PASSWORD` no admite parámetro, así que la comilla simple, la barra, el espacio, la clave corta y la vacía se rechazan antes del DDL— y el nombre del rol. FUERA DE ALCANCE: `set_config('app.current_role', …, true)` por transacción y la RLS de rol son AC-FTEN-21 y §4.8 — oráculo: CI [AC-FTEN-03]
- [x] (P1) La BD `control` contiene registro de tenants (slug→bd, plan_id, modo, estado), entitlements/billing SaaS ACOTADO a E1 (= `plan_id` + las 3 tablas de entitlements según Pregunta 3; cero DDL o lógica de cobro Stripe/página de precios — eso es E2, §3 FUERA de E1, y billing no figura en los ganchos §4.9), invitaciones de tenant nuevo, grants de soporte y la tabla de agregados técnicos del exportador (salud, adopción, EEVD agregada, backlog de sync), y un test de schema prueba que `control` no tiene tablas de dominio operativo y que el payload del exportador cumple su schema fijo; caso de rebote: inyectar una columna de dinero/tarifa/cliente al agregado hace fallar el test de schema (centinela 14); la OPERACIÓN del push del job es AC-FTEN-20. Evidencia: `db/migraciones-flota/control/0001_plano_de_control.sql` —el primer destino `control/` del proyecto— y 8 pruebas contra el cluster real en `db/flota/suite-bd/control.test.mjs`. La regla de `control` es lo que NO tiene, así que la prueba principal no es «existen estas tablas» sino DOS listas LITERALES: qué tablas hay (las 9 de E1, nombradas una por una) y qué columnas tiene el agregado del exportador (las 12 del schema fijo). Una lista literal es lo único que se rompe cuando alguien agrega algo sin pensarlo, que es exactamente el caso del centinela 14; encima corre un guard de vocabulario que rechaza cualquier nombre de columna que huela a dato comercial (`clp|monto|tarifa|precio|costo|factura|liquidacion|cliente|rut`). El rebote se EJERCE de verdad: se inyecta `ingresos_mes_clp bigint` en `agregados_tecnicos`, se comprueba que las dos verificaciones se ponen en rojo, se revierte y se comprueba que el cluster volvió a estar sano — sin ejercerlo, «el test de schema falla» sería una afirmación sobre código que nadie corrió. Se verifica además que `control` NO tenga `tenant_info` (se estaría haciendo pasar por un tenant) ni ninguna tabla de dominio del tenant, nombradas una por una. Entitlements en `control` por respuesta del dueño a la Pregunta 3 (09-ago-2026), con `tenant_feature_overrides.motivo` NOT NULL y no vacío —una excepción sin razón escrita es una excepción sin dueño— y `grants_soporte` con vencimiento obligatorio y posterior al otorgamiento, porque un grant sin vencimiento es un acceso permanente con otro nombre. `tenants.estado ∈ {activo, suspendido, archivado}` es el enum cerrado contra el que se escribe la conducta de AC-FTEN-05, y un CHECK impide que `bd` y `slug` diverjan: un tenant apuntando a la base de otro sería el cruce que el §4.1 hace imposible por construcción, deshecho en una fila de una tabla. Los campos del agregado cuya fuente operativa todavía no existe quedan NULL declarado y no cero inventado (`eevd_semanal` nace en el hito c, `backlog_sync_max_min` en el hito e): un cero diría «medido y da cero». HALLAZGO del camino: `schema_migrations` la creaba `tenant/0001`, así que el destino `control` —que tiene sus propias migraciones y ninguna de ellas— se caía en su primera corrida; la contabilidad del runner pasó a crearla el RUNNER, que es de quien es — oráculo: CI [AC-FTEN-04]
- [ ] (P1) El ruteo resuelve subdominio → lookup en `control` → pool de conexiones de SU BD vía PgBouncer multi-database con límite de pool POR tenant declarado en la config generada; verificación: request al subdominio de A opera sobre la BD de A (responde su `tenant_info.slug`) y la config de pools contiene un límite por cada tenant registrado; casos de rebote (DESBLOQUEADOS por la Pregunta 9, respondida el 09-ago-2026): subdominio SIN tenant en `control` ⇒ **404** —jamás un 403 ni un 404 distinto del de recurso ajeno: no se revela si el subdominio existe (§0 HTTP)—; tenant con `estado = 'suspendido'` ⇒ **503** con página propia en es-CL y CERO acceso a su base (ni una conexión abierta contra `t_<slug>`); tenant con `estado = 'archivado'` ⇒ **404**, como si no existiera. `control.tenants.estado ∈ {activo, suspendido, archivado}` es un enum cerrado — oráculo: CI [AC-FTEN-05]
- [x] (P1) El linter de esquema/migraciones (`db/flota/lint-migraciones.mjs`) exige en TODA tabla de dominio: `tenant_id uuid NOT NULL` + CHECK contra la constante de la BD (`tenant_info`) + índice + FK compuesta `(tenant_id, id)` + `COMMENT ON TABLE` con clase `PLANIFICACIÓN|CAPTURA`; caso de rebote: una migración de fixture que omite cualquiera de las cinco exigencias produce exit ≠ 0 (build rojo). El CHECK exigido es `CHECK (tenant_id = tenant_actual())`: la forma literal del §4.1 —`(SELECT id FROM tenant_info)`— la RECHAZA PostgreSQL («cannot use subquery in check constraint», verificado contra 18.4), y la función IMMUTABLE con el uuid horneado en la provisión es además la única segura ante `pg_restore` del offboarding. La exigencia de índice tiene dos mitades porque la primera sola no atrapa nada —`UNIQUE (tenant_id, id)` ya es un índice encabezado por tenant_id, lo destapó un mutante que no lograba ponerse rojo—: índice encabezado por `tenant_id` Y cada FK compuesta cubierta por un índice que la encabece. Las tablas de `control/` no llevan tenant_id por definición y solo se les exige la clase. Las exenciones se declaran en la propia migración y el linter las CUENTA e imprime, jamás silenciosas. Evidencia: 15 mutantes en `db/flota/lint-migraciones.test.mjs`, uno por exigencia más los de frontera (clase CAPTURA válida, tabla de control, exención contada, dos tablas en un archivo sin que la primera tape a la segunda, y «sin migraciones» declarado en vez de verde vacuo) — oráculo: CI [AC-FTEN-06]
- [x] (P1) El runner de migraciones itera el canario sintético `t_canary` PRIMERO y luego todas las BD tenant, con `schema_migrations` por BD y ejecutado con rol `migrator` separado (la app no tiene ownership); caso de rebote: una BD tenant rezagada ⇒ exit ≠ 0 y el deploy no se declara verde (centinela 13). Evidencia: `db/flota/migrar.mjs` (`aplicar | verificar [--dir=…]`) recorre `t_canary` → `tenant_template` → cada `t_<slug>` viva, y corre dentro de `db/flota/gate.sh --full` en CADA corrida. El rol `migrator` es LOGIN NOSUPERUSER NOCREATEDB NOCREATEROLE NOBYPASSRLS y es el DUEÑO de la plantilla y de cada BD de tenant, fijado al crearlas y no por un ALTER posterior: desde PostgreSQL 15 el esquema `public` pertenece a `pg_database_owner`, así que el dueño de la base es lo único que habilita crear tablas ahí. Una base de dueño ajeno detiene el runner NOMBRÁNDOLA, en vez del «permission denied for table schema_migrations» sin sujeto que costó su primera corrida. 5 pruebas contra el cluster real en `db/flota/suite-bd/migrar.test.mjs` (orden aplicado == orden declarado y ninguna base viva fuera del recorrido · `schema_migrations` propia por BD, todas en la última versión · `current_user` = `migrator` en cada base más los atributos del rol y el ownership · base de dueño ajeno ⇒ rebote con su nombre y cluster intacto después · centinela 13) y 8 mutantes puros en `db/flota/migrar.test.mjs`, que corren sin cluster: el orden con el canario ya vivo, sin existir todavía y sin duplicarlo, y el umbral del centinela con UNA sola base rezagada entre varias al día —el caso que de verdad se escapa—, con «adelantada» tratada como rojo y `esperada: null` declarado vacío en vez de verde. El centinela 13 se ejerce verde→rojo→verde: `verificar --dir=` contra un directorio con una migración de más sale ≠ 0 nombrándola, y con el directorio real vuelve a 0. ARRANQUE EN FRÍO declarado: si el canario todavía no existe, la plantilla se adelanta UNA vez para que el canario pueda nacer de ella (un cluster vacío no tiene estado previo que una migración pueda romper); de la segunda corrida en adelante el canario va primero, que es cuando importa. FUERA DE ALCANCE: el enganche en `scripts/deploy.sh` («abortar ANTES de sustituir la versión servida», §9.1) es del hito 0, que todavía no creó ese script — `verificar` es el modo que ese paso consume y ya está ejercido en el gate; el rol `app_t_<slug>` sin ownership y su CONNECT acotado son AC-FTEN-03. AMPLIADO el 09-ago-2026: `verificar` suma al rezago el invariante de identidad —una base al día pero sin `tenant_info` sembrada está tan rota como una atrasada, porque el CHECK de cada tabla de dominio compara contra el centinela de la plantilla y pasa—, con su prueba de cluster (crear una BD desde la plantilla sin sembrarla deja `verificar` en exit ≠ 0 nombrándola, y quitarla lo devuelve a 0) — oráculo: CI [AC-FTEN-07]
- [x] (P1) Toda PK de tabla de dominio es uuid poblada con UUIDv7 generado EN SERVIDOR (jamás bigint, jamás v4), verificado en sus DOS mitades: bits de versión 7 en filas creadas Y generación server-side — pgTAP de que toda PK de dominio tiene DEFAULT con la función servidor de UUIDv7 en el catálogo, más test de INSERT sin PK ⇒ el servidor la genera (§0) y las tablas de mutación offline llevan `client_uuid` UUIDv7 con `UNIQUE(tenant_id, client_uuid)`; caso de degradación: doble INSERT con el mismo `client_uuid` vía `ON CONFLICT DO NOTHING` deja exactamente 1 fila y ningún error. Evidencia: `db/flota/pgtap/0001_uuidv7_y_client_uuid.sql`, 16 pruebas en verde, corridas por `db/flota/pgtap.mjs` dentro de `db/flota/gate.sh --full` contra `t_canary` —un tenant de verdad, provisionado desde la plantilla igual que los demás, así que el catálogo que se verifica es el que le va a tocar a un cliente y no el de una base de juguete— y siempre dentro de una transacción que se revierte, porque las pruebas insertan filas reales: es la única forma de probar que la PK la pone el servidor. Las DOS mitades, separadas a propósito: el CATÁLOGO (toda PK de dominio es uuid, de UNA columna, con DEFAULT `uuidv7()`, y `uuidv7()` es la NATIVA de `pg_catalog` y no una implementación propia que alguien pudiera cambiar sin que nadie se entere) y el COMPORTAMIENTO (INSERT sin PK ⇒ el servidor la genera y el nibble de versión es 7). Ninguna sola alcanza: el catálogo no prueba que funcione y el INSERT solo prueba la tabla que se probó. Y una tercera que ninguna da: el catálogo ejercitado NO está vacío —`cmp_ok(count, '>', 0)` sobre las PKs de dominio y sobre las tablas con `client_uuid`—, porque este es justamente el test que se escribe antes de que existan las tablas que tiene que revisar. `tenant_info` queda exenta del catálogo (su fila la siembra la provisión, no un DEFAULT) y por eso se verifica APARTE que su id también sea UUIDv7. Idempotencia: toda tabla con `client_uuid` lo tiene `uuid` y con `UNIQUE (tenant_id, client_uuid)` —con el `tenant_id` adelante, o el índice no serviría el día de una consolidación (§4.1)—, y el caso de degradación se ejerce completo: el mismo `client_uuid` dos veces con `ON CONFLICT DO NOTHING` no rebota y deja 1 fila, mientras que SIN `ON CONFLICT` rebota con 23505, que es lo que prueba que el índice está vivo y no es decorativo. El arnés lleva sus propios 6 mutantes en `db/flota/pgtap.test.mjs`, porque es un intérprete de la salida de otro programa y esa es la clase de pieza que se rompe hacia el verde: se prueba que `not ok` no se lea como `ok` (es subcadena), que los diagnósticos se conserven, y que «cero pruebas» sea ROJO y no silencio — el conteo sale de las líneas `ok`/`not ok` y NO del plan, porque un `finish()` que nunca llega dejaría el `1..16` escrito y ni una prueba corrida. HALLAZGO: esta suite fue la que destapó que `t_canary` estaba sin identidad (corregido en AC-FTEN-02/AC-FTEN-07) — oráculo: CI [AC-FTEN-08]
- [x] (P1) Tipado de dinero: toda columna de monto es CLP `bigint` entero y `round_clp()` existe como ÚNICA función de redondeo (§0 fila Dinero); el pgTAP corre contra el catálogo de columnas de dinero y FALLA si el catálogo ejercitado está vacío (verde vacuo prohibido) — al cierre de este módulo el catálogo ya contiene columnas reales (`parametros.tarifa_kwh_clp`, `parametros.precio_diesel_litro_clp`); caso de rebote: una migración de fixture con una columna de monto `numeric`/`float` ⇒ gate rojo; la política RLS de dinero es AC-FTEN-21. Evidencia: `db/migraciones-flota/tenant/0004_dinero.sql` crea `round_clp(numeric) returns bigint`, IMMUTABLE y STRICT; 12 pruebas pgTAP en `db/flota/pgtap/0003_dinero.sql` y 7 mutantes nuevos del linter en `db/flota/lint-migraciones.test.mjs`. El criterio de «columna de monto» es el SUFIJO `_clp`, y corre en los dos lados: el linter lo atrapa ANTES de que un `numeric` llegue a una base (mutantes con `numeric`, `numeric(12,2)`, `double precision`, `real`, `money` e `int`, más el positivo en `bigint` para que el guard no sea un no-op al revés) y el pgTAP lo atrapa en el catálogo REAL, incluido lo que alguien altere a mano. La primera prueba de la suite es que el catálogo NO esté vacío: este test se escribió cuando no había ninguna tabla de montos y sin ella habría estado verde meses sin mirar nada — hoy ejercita `parametros.tarifa_kwh_clp` y `parametros.precio_diesel_litro_clp`, verificadas por nombre. `round_clp` se prueba en su comportamiento (el 0,5 se aleja del cero en los dos signos, el cero es cero, STRICT no inventa un redondeo sin monto) y en su UNICIDAD: cero funciones del esquema cuyo nombre contenga `round` o `redonde` fuera de ella, porque un segundo redondeo hace que dos partes del sistema dejen de coincidir por un peso y nadie sepa cuál manda. HALLAZGO del camino: la primera versión del linter leía «bigint not null» como el tipo de la columna y hacía fallar justamente al positivo — el tipo se recorta ahora hasta donde termina el tipo (`double precision`, `numeric(12,2)`) y no hasta la coma — oráculo: CI [AC-FTEN-09]
- [ ] (P1) `tenant_theme` y `tenant_terminology` existen con sus invariantes: accent_color con contraste <4.5:1 es RECHAZADO, contra el fondo QUE CORRESPONDE (DESBLOQUEADO por la Pregunta 7, respondida el 09-ago-2026): la plataforma deriva una variante del acento por tema y valida CADA UNA contra el fondo de ESE tema —la clara contra el fondo claro, la oscura contra el fondo oscuro, ambas ≥ 4,5:1—, y el fixture de rechazo es un acento que, derivado para el tema oscuro, no llega a 4,5:1 contra el fondo oscuro. La lectura que esta spec proponía (rechazar si falla contra CUALQUIERA de los dos fondos) quedó descartada por el dueño y la razón se conserva porque es la que impide volver a proponerla: el producto del ratio de un color contra blanco por su ratio contra negro es exactamente 21, así que exigir ≥4,5:1 contra ambos deja pasar solo acentos con ratio entre 4,50 y 4,67 contra blanco, una rendija donde casi ningún color de marca entra; term_key con singular o plural ausente, largo fuera del CHECK por tipo (navegación ≤12, títulos ≤24, descripciones ≤40), caracteres prohibidos `# $ % ; < = >` o término de sistema/auditoría ⇒ rebote con error — oráculo: CI [AC-FTEN-10]
- [x] (P1) Entitlements con 3 tablas (`features` con lookup_key y module, `plan_features`, `tenant_feature_overrides` con enabled y motivo), resolución efectiva = `override ?? plan` en una función/vista única probada en AMBOS sentidos conforme a la fórmula LITERAL del §4.4: override OFF sobre feature del plan ⇒ OFF, y override ON ⇒ ON INCLUSO sobre feature fuera del plan (el guard que impide ENCENDER fuera de plan es regla de la pantalla «Funciones» del hito g, §5.5 — no de la función de resolución); límites cuantitativos como columnas del plan; el preset de modo y la conmutación son AC-FTEN-22. Evidencia: `db/migraciones-flota/control/0002_entitlements_efectivos.sql` —`entitlement_efectivo(uuid, text)` y la vista `entitlements_efectivos`, la misma fórmula en forma de conjunto para armar el snapshot de config de una sola lectura— y 5 pruebas en `db/flota/suite-bd/control.test.mjs`. Se prueba en AMBOS sentidos porque una implementación que solo mire el plan pasa la mitad de los casos: sin override el efectivo ES el plan (y una feature que ni existe resuelve OFF, no error), override OFF sobre feature DEL plan ⇒ OFF, y override ON sobre feature FUERA del plan ⇒ ON. Esto último es deliberado y está escrito en la propia función: el guard comercial que impide ENCENDER fuera de plan es de la pantalla «Funciones» del hito g (§5.5), y mezclarlo con la resolución haría imposible el piloto acordado con un cliente — que es justamente por lo que `tenant_feature_overrides.motivo` es obligatorio. La vista y la función se comparan sobre CADA feature del tenant y no sobre una elegida a mano: dos fuentes de verdad para la misma fórmula es la forma más cara de tener un bug. Los límites cuantitativos se verifican como COLUMNAS de `planes` (`limite_vehiculos`, `limite_entregas_mes`): un límite es un número y no un interruptor, y si viviera como feature, «1 vehículo» y «300 entregas/mes» del plan Partida serían dos booleanos que no dicen cuánto. Las 3 tablas viven en `control` por respuesta del dueño a la Pregunta 3 (09-ago-2026), y el efectivo se congela en el snapshot de config (Pregunta 4), con lo cual el runtime del producto nunca vuelve a consultar `control` por esto — oráculo: CI [AC-FTEN-11]
- [x] (P1) `vertical_template`(vertical, terminologia, motivos[], checklists[], cargo_types[], config_ev, meta_eevd), `grupos`(padre_id) como árbol único sin ciclos y `parametros` por tenant (incl. reserva_pct, factor_consumo default 0,85, tarifa_kwh_clp, precio_diesel_litro_clp, bultos_max_sin_receptor) existen en la plantilla con pgTAP de estructura; caso de rebote: `grupos` con ciclo o `meta_eevd` ausente en un vertical sembrado ⇒ error. Evidencia: `db/migraciones-flota/tenant/0003_verticales_grupos_y_parametros.sql` y 33 pruebas pgTAP en `db/flota/pgtap/0002_verticales_grupos_y_parametros.sql`, en verde dentro de `gate.sh --full`. Los dos rebotes del AC se ejercen contra la BD: `meta_eevd` es NOT NULL y un vertical sembrado sin ella sale 23502; los ciclos se atrapan con un trigger que RECORRE la rama hacia arriba —una FK no alcanza, porque cada fila de un ciclo es válida por separado— y se prueban en sus tres formas (grupo que es su propio padre, ciclo de dos saltos y de tres), más el positivo de reparentar una rama a un nodo que no es su descendiente, para que el guard no sea un no-op al revés. DECISIONES DECLARADAS: (a) `motivos` y `cargo_types` son `text[]` (listas planas de códigos) y `checklists`/`terminologia`/`config_ev` son `jsonb` (estructurados); (b) `parametros` es de UNA sola fila con el patrón `unica boolean UNIQUE CHECK (unica)` de `tenant_info` —en una BD por tenant, «por tenant» ES «una fila»— y una segunda fila sale 23505; (c) `tarifa_kwh_clp` y `precio_diesel_litro_clp` son las primeras columnas de monto REALES del proyecto, `bigint` entero (§0, §4.8), y son las que ejercita el catálogo de AC-FTEN-09; (d) `factor_consumo` es NULLABLE y el DDL NO hornea el 0,85: el §0 dice «factor_consumo 0,85 y override en `parametros`», así que el valor canónico vive UNA vez en `constants.ts` y esta columna es el override —copiarlo al SQL sería exactamente el número mágico duplicado que el grep-gate de AC-FTEN-01 atrapa, y hay un pgTAP que verifica que la columna no tenga DEFAULT—. AMPLIADO el 09-ago-2026 con la respuesta del dueño a la Pregunta 5, que CERRÓ la lista de claves de `parametros` para E1 en OCHO: `db/migraciones-flota/tenant/0005_parametros_lista_cerrada.sql` agrega las tres que faltaban (`anticipacion_vencimiento_dias`, `tolerancia_eta_minutos`, `periodicidad_liquidacion` con default `'semanal'`) y aplica dos consecuencias que NO eran cosméticas: (1) el CHECK de `bultos_max_sin_receptor > 0` de la 0003 habría REBOTADO el valor que el dueño eligió —cero, «siempre foto»—, porque la semántica es «cuántos bultos se perdonan sin encuadre» y el cero es legítimo, no un vacío; ahora es `>= 0`; (2) el piso de 15 minutos del Anexo B vive como CHECK de `tolerancia_eta_minutos` y el valor dictado (20) vive en el seed, tal como el dueño lo pidió. Un pgTAP `bag_eq` verifica que la tabla tenga EXACTAMENTE las ocho claves y ninguna de más, que es lo que hace de «lista cerrada» algo verificable y no una intención. Los valores siguen siendo seeds del hito (g). La APLICACIÓN de la visibilidad por grupos ya no está bloqueada —la Pregunta 12 se respondió el 09-ago— pero sigue siendo AC-FTEN-27: acá está el árbol y su invariante, y ninguna superficie asume grupos todavía — oráculo: CI [AC-FTEN-12]
- [ ] (P1) La configuración por tenant es versionada y congelada: todo cambio genera una versión nueva referenciable por `config_version_id` sin mutar la anterior, que queda consultable intacta (el consumo por `turno.config_version_id` y «cambios aplican al turno siguiente» se ejercitan end-to-end en el hito c); caso de degradación: editar config con una versión ya referenciada jamás altera esa versión — oráculo: CI [AC-FTEN-13]
- [x] (P1) Ganchos VIVOS de E1 presentes en la plantilla: `cargo_type`, `attribute_definition` versionada + trigger que valida `attrs` contra la definición VIGENTE, `stop_requirement`, `reading` genérica (sin seeds de frío; nace APPEND-ONLY — REVOKE UPDATE/DELETE al rol de app + trigger, §4.6/§7.4 — y con `valor_int` SIN CHECK de rango: el CHECK 0–100 vive SOLO en la proyección `vehiculos.soc`, trigger del hito c, §0 fila SOC), `instrument`, `vehicle_certification`, `lot`(tlc, vencimiento) y `reference_document`(tipo DTE 33|39|52|61, folio, emisor) con `UNIQUE(tipo, folio, emisor)` — ESTE módulo es su único creador (§4.9 los pone en la misma fila de ganchos VIVOS y toda tabla nace en `tenant_template`); el módulo 03 los CONSUME (FK `items.lote_id`, creación de filas de `reference_document` desde F2) y no los crea. Las clases `COMMENT ON TABLE` de `lot` y de `reference_document` son **CAPTURA** ambas, por respuesta del dueño del 08-ago-2026 a las Preguntas 11 y 13 (antes bloqueaban este AC); casos de rebote: INSERT de planificación con attrs no conforme a la definición vigente ⇒ RAISE; UPDATE/DELETE sobre `reading` como `app_t_<slug>` ⇒ 42501 (la parte del centinela 6 que corresponde a las tablas nacidas aquí, §9.3.6); pgTAP verifica la AUSENCIA de constraint de rango sobre `reading.valor_int` y una migración de fixture que lo agrega ⇒ gate rojo (§9.3.4); y un test activa un vertical sintético SOLO con INSERTs (cero migraciones nuevas) quedando el trigger validando contra la definición insertada. Evidencia: `db/migraciones-flota/tenant/0006_ganchos_vivos.sql`, 29 pruebas pgTAP en `db/flota/pgtap/0004_ganchos_vivos.sql`, 3 pruebas con el rol de app real en `db/flota/suite-bd/hechos.test.mjs` y 4 mutantes nuevos del linter. El vertical sintético se activa con INSERTs y CERO migraciones —la métrica 4 del §2 y la promesa entera del §4.9— y el trigger `validar_attrs()` se engancha a cualquier tabla con `attrs` con una línea, igual que la RLS de dinero de AC-FTEN-21; se ejerce sobre una tabla FIXTURE porque ninguna tabla de ESTE módulo lleva `attrs` (las de los hitos c y d sí). El trigger tiene DOS mitades y las dos se prueban: toda clave presente tiene que estar definida y ser del tipo declarado —clave no definida, tipo equivocado y opción fuera del conjunto rebotan, porque si pasaran `attrs` sería el jsonb libre que el §4.9 prohíbe con todas las letras— y todo atributo OBLIGATORIO vigente tiene que estar, sin lo cual exigir algo no serviría de nada. La definición es VERSIONADA y se prueba superseder: una definición no se edita, porque los `attrs` ya escritos no pueden volverse inválidos retroactivamente contra una definición que nadie aceptó cuando se escribieron (§4.2). LA AUSENCIA del CHECK de rango sobre `reading.valor_int` se verifica por partida doble: pgTAP mira el catálogo y confirma que no hay ninguna restricción sobre esa columna, y el linter de migraciones tiene una regla propia —con su positivo y tres mutantes (rango en línea, piso, restricción con nombre), más el mutante de que la MISMA restricción en otra tabla NO dispara, porque `vehiculos.soc` sí la lleva en el hito c—. Un rango ahí haría que una sonda descalibrada REBOTARA la captura del chofer, que es justo lo que el §4.2 prohíbe; el test con el rol de app inserta una lectura de 1350 décimas de por ciento y verifica que ENTRA. `reference_document` se verifica además por lo que NO tiene: ninguna columna que huela a emisión (`xml`, `ted`, `track`, `timbre`, `caf`, `estado_sii`), porque la app JAMÁS emite DTE (§7.3, art. 97 N°4 CT). Y los ganchos nacen SIN seeds —ni de frío, ni de carga, ni de `pin_destinatario`—, verificado contando filas en la plantilla: un seed acá convertiría una decisión del tenant en una decisión nuestra. DECISIÓN DECLARADA: `magnitud` es una tabla nueva que el §4.6 pide implícitamente («magnitud FK»), y es lo que hace que `reading` sea UNA tabla para odómetro, SOC, temperatura y humedad en vez de una columna por magnitud — oráculo: CI [AC-FTEN-14]
- [ ] (P1) Ganchos DDL-only sin activar: `thermal_profile`, `alarm_rule` y `disposition`+auto-release existen como esquema sin seeds ni UI — `disposition` nace APPEND-ONLY (REVOKE UPDATE/DELETE al rol de app + trigger; caso de rebote: UPDATE/DELETE como `app_t_<slug>` ⇒ 42501; §4.6/§7.4, §9.3.6) —, con el CHECK VIVO de la matriz de honestidad (activar `alarm_rule` `cumulative` o feature de compliance en tenant cuya única fuente sea `declarada` ⇒ rebote), el trigger de `excursion` VIVO pero inerte (readings insertadas sin alarm_rules sembradas ⇒ 0 excursiones) y la interfaz `ProveedorTelemetria` con única implementación `declarada` registrada — oráculo: CI [AC-FTEN-15]
- [x] (P1) Reglas estáticas en CI con patrón cierto: fallan ante superuser/BYPASSRLS/ownership en la conexión de la app, SET de sesión (solo SET LOCAL permitido) y consultas cross-database en runtime del producto; la segregación de cache keys/colas/jobs/logs por tenant (§7.2) se vuelve ENFORCEABLE por convención que esta spec fija como mecanismo de verificación: acceso a cache, colas, jobs y logging SOLO vía el wrapper tenant-scoped único del core, con grep bloqueante de usos directos de las APIs crudas fuera del wrapper (mismo patrón que el grep de `railway` fuera de `scripts/deploy.sh`, §9.1); backups por BD tenant documentados en el runbook del repo; caso de rebote: fixture que introduce cualquiera de esos patrones — incluido un uso directo de API cruda de cache/cola/job/log fuera del wrapper — pone el gate rojo. Evidencia: `db/flota/gate-reglas-estaticas.mjs`, dentro de `db/flota/gate.sh`, con 12 fixtures en su suite. Estas reglas van a estar años sin disparar porque el código que las viola FUNCIONA —anda perfecto hasta el día que cruza datos de dos tenants—, así que cada una se ejerce contra el caso que atrapa Y contra un positivo que impide que sea un no-op al revés: un patrón demasiado angosto se ve exactamente igual que uno que funciona. Las cuatro reglas: rol privilegiado en la conexión de la app (`superuser`, `BYPASSRLS`, `flota_admin`, `migrator`); SET de sesión sobre `app.*` —con el positivo de que `set_config(..., true)` y `SET LOCAL` NO disparan, que es lo que distingue la regla de una que rompe todo el código correcto—; `dblink`/`postgres_fdw`/`CREATE SERVER`; y la API cruda de cache, colas, jobs o logging fuera del wrapper. LA CONVENCIÓN QUE ESTA SPEC FIJA: esos cuatro planos se tocan SOLO a través del wrapper tenant-scoped único del core (`packages/nucleo-comun/src/tenant-scope.ts`), y el wrapper es el ÚNICO exento — si no lo fuera, alguien tendría que apagar la regla para poder escribirlo, y una regla apagada no protege nada. La familia canónica de constantes queda exenta SOLO de la regla 1, porque el §0 la obliga a NOMBRAR `migrator` y `app_t_<slug>` y es dato sin conexiones; hay un fixture que verifica que la exención es de una regla y no del archivo, para que el canónico no se vuelva el mejor escondite del repo. Los comentarios no disparan: una regla que se pisa con su propia explicación es una regla que alguien apaga a la semana. ALCANCE DECLARADO: mientras `apps/flota` no exista, el gate lo DICE en cada corrida en vez de reportar un verde que no probó nada, y sus reglas igual están vivas contra los fixtures. La quinta exigencia del AC —backups por BD tenant documentados en el runbook— no es un grep sobre código sino sobre un documento, así que vive en `db/flota/gate-documentos.mjs` como una exigencia más del runbook de AC-FTEN-25 — oráculo: CI [AC-FTEN-16]
- [x] (P2) Offboarding de tenant: script que entrega la BD completa del tenant (`pg_dump` de SU BD) y test que la restaura standalone verificando conteos contra la original (portabilidad Ley 21.719, métrica de plataforma 7). Evidencia: `db/flota/offboarding.mjs` y 6 pruebas contra el cluster en `db/flota/suite-bd/offboarding.test.mjs`. El volcado sale en SQL PLANO con `--no-owner --no-privileges` a propósito: lo que se le entrega al tenant tiene que restaurarse en cualquier PostgreSQL del mundo con los roles de quien lo reciba, y un `-Fc` con nuestros `migrator`/`app_t_<slug>` adentro sería un archivo que solo nosotros podemos abrir — lo contrario de portabilidad; hay una prueba que verifica que el archivo NO traiga dueños ni permisos nuestros. La restauración crea una base VACÍA y no la deriva de `tenant_template`, para que lo probado sea que el volcado se basta a sí mismo, y corre con `ON_ERROR_STOP=1`, que es la diferencia entre «restauró» y «corrió y algunas cosas fallaron» — psql sigue adelante por omisión y termina en 0 con la base a medias. Los conteos se comparan tabla por tabla sobre SEIS tablas sembradas, con un aserto previo de que ninguna esté vacía: comparar ceros no prueba nada. Y esta suite ejerce la decisión de diseño más delicada del módulo: la restaurada sigue teniendo `tenant_coherente()` verdadero y sus CHECK vivos —rechaza una fila de otro tenant con 23514—, que es exactamente lo que NO habría pasado si `tenant_actual()` leyera `tenant_info` en vez de traer el uuid horneado como literal: las funciones se restauran ANTES que los datos, así que ese CHECK habría fallado fila por fila durante el COPY. El append-only del §7.4 también viaja: un UPDATE sobre `eventos` en la base restaurada sigue saliendo 42501 — oráculo: CI [AC-FTEN-17]
- [x] (P1) `docs/criterios-kiloruta.txt` extraído UNA vez desde la spec de KiloRuta con IDs cerrados `KR-01…KR-63` y N total explícito (N = 63), y la lista APROBADA por Alexis el 08-ago-2026 antes de continuar el hito (a), con sus 7 decisiones firmadas en el mismo acto (KR-29 y KR-41 entran a E1 como AC-FRUT-22 y AC-FVEH-22; KR-34, KR-48 y la cláusula «solo primera apertura» de KR-17 descartados con nota; KR-38 y la cláusula «próximo servicio por km» de KR-51 diferidos); el congelado lo custodia `db/flota/gate-criterios-kiloruta.mjs` dentro del gate — N declarada == criterios presentes, IDs contiguos y sin repetir, clases del conjunto cerrado y firma del dueño presente —, con sus mutantes en `db/flota/gate-criterios-kiloruta.test.mjs` — oráculo: humano [AC-FTEN-18]
- [ ] (P1) `docs/matriz-kiloruta.md` como tabla MD `ID | tabla/constraint | test (ruta::nombre)` con gate mecánico de tres verificaciones: count(filas)==N, cada ID aparece exactamente una vez, y cada test referenciado existe en el repo; caso de rebote: un ID duplicado, faltante o un test inexistente ⇒ gate rojo — oráculo: CI [AC-FTEN-19]
- [x] (P1) El job exportador a `control` EXISTE y corre en CI (§4.1: agregados técnicos empujados por un job exportador desde cada BD tenant): ejecutarlo contra una BD tenant provisionada produce/actualiza la fila de agregados de ese tenant en `control` cumpliendo el schema fijo; los campos cuya fuente operativa aún no existe al cierre de este módulo (EEVD agregada — la vista `eevd_semanal` nace con el primer módulo operativo, §2 — y backlog de sync, hito e) quedan NULL/pendiente hasta su hito (ver Dependencias); el rebote del centinela 14 se conserva en AC-FTEN-04. Evidencia: `db/flota/exportar.mjs`, que corre dentro de `db/flota/gate.sh --full` en CADA corrida, con 7 pruebas contra el cluster en `db/flota/suite-bd/exportar.test.mjs` y 5 mutantes puros de la ventana en `db/flota/exportar.test.mjs`. CÓMO SE HONRA LA PROHIBICIÓN DE CROSS-DATABASE (§4.1, §7.2): el job abre DOS conexiones separadas —una a la base del tenant, otra a `control`—, lee de la primera y escribe en la segunda; ninguna consulta ve las dos bases a la vez, que es exactamente lo que un dblink o un FDW harían y lo que el maestro prohíbe. Y no es runtime del producto: es un job. Cadencia de 5 minutos por respuesta del dueño a la Pregunta 8 (09-ago-2026), viviendo en `EXPORTADOR.cadencia_min` de la familia canónica de constantes y no suelta en el script; la ventana se ALINEA al reloj, así que dos corridas del mismo tramo actualizan UNA fila en vez de crear dos —probado— y el borde superior pertenece al tramo siguiente, para que una métrica del minuto exacto no se cuente dos veces. Se recorre el REGISTRO de `control` y no las bases del cluster: una base que existe pero no está registrada no es un tenant sino un resto de algo, y exportar sus datos sería inventarle un dueño en el panel cross-tenant; un tenant registrado cuya base no existe se NOMBRA y deja el job en exit ≠ 0, jamás se saltea callado (§10), y un tenant no `activo` no se exporta. NULL declarado y no cero inventado en las dos direcciones: `eevd_semanal` y `backlog_sync_max_min` quedan NULL hasta su hito, y `errores_sync_pct` es NULL cuando no hubo ninguna métrica en la ventana —«cero errores» y «no se midió» son cosas distintas y el panel del §10 las pinta distinto—, mientras que los conteos de distintos SÍ dan cero, porque ahí sí se contó — oráculo: CI [AC-FTEN-20]
- [x] (P1) Política RLS de dinero como PATRÓN ejecutable: `AS RESTRICTIVE` declarada `FOR SELECT` únicamente, exige `app.current_role NOT IN ('chofer','responsable_carga')` con `set_config(..., true)` SET LOCAL por transacción (§4.8); la suite del módulo la prueba con el rol de app real sobre una tabla FIXTURE de montos (solo del harness de test — ninguna tabla de montos del §4.8 nace en este módulo) y FALLA si no ejercita ≥1 tabla (verde vacuo prohibido); casos: con rol `chofer` el SELECT ⇒ 0 filas PERO el INSERT de una captura con costo NULL pasa sin rebote (FOR SELECT únicamente — una RESTRICTIVE total rebotaría el cierre de recarga del chofer, §4.8); los hitos c/f instancian la política sobre las tablas reales de montos (ver Dependencias). Evidencia: `aplicar_rls_de_dinero(regclass)` en `db/migraciones-flota/tenant/0004_dinero.sql` aplica el patrón a cualquier tabla de montos con UNA línea, y 7 pruebas en `db/flota/suite-bd/dinero.test.mjs` lo ejercen con el rol `app_t_<slug>` REAL sobre una tabla fixture del harness — esto no se puede probar con pgTAP, que corre como superusuario y a quien la RLS no se le aplica. Son DOS políticas y ninguna es prescindible: una PERMISSIVE de base, porque una RLS sin ninguna permisiva le niega la tabla a cualquiera y no solo al chofer, y la RESTRICTIVE del §4.8 declarada `FOR SELECT` únicamente —verificado leyendo `pg_policy.polcmd = 'r'`, no por confianza—. Casos: con `operador` los montos se ven (sin esta mitad positiva todo lo demás sería verde por una tabla vacía); con `chofer` y con `responsable_carga` el SELECT da 0 filas; y el chofer INSERTA su captura con `costo_clp` NULL sin rebote y la fila existe de verdad, leída después como `operador` — que es la corrección explícita del maestro contra una RESTRICTIVE total. DECISIÓN DECLARADA: la falla va hacia el CIERRE. `app.current_role NOT IN (…)` a secas es VERDADERO cuando nadie declaró el rol, así que una transacción que se olvidó vería TODOS los montos; el §7.2 obliga a declararlo en cada transacción, o sea que no declararlo es un bug, y ante un bug una política de dinero niega. HALLAZGO: `current_setting(…, true)` devuelve CADENA VACÍA —no NULL— después de que termina la transacción que hizo el SET LOCAL, así que el `nullif` sobre `''` no es adorno: sin él la transacción siguiente del pool heredaría el permiso, y hay una prueba que lo fija — oráculo: CI [AC-FTEN-21]
- [x] (P1) `tenants.modo ∈ {mi_flota, daas}` opera como preset del MISMO sistema de entitlements con el mapeo CERRADO del §3: con `mi_flota`, el entitlement efectivo de tarifas, liquidación por cliente, portal del contratante y facturación resuelve OFF (ocultos — la contracción de UI/manifest se verifica en los hitos e/g, §5.5, §10 tenant C); con `daas` no aplica ese recorte; caso de rebote/degradación: conmutar mi_flota→daas→mi_flota no ejecuta DELETE ni pierde una sola fila de config o dominio (base del centinela 11; el centinela completo con la empresa implícita intacta se cierra cuando exista `empresas_cliente`, hito d/f, §9.3.11). Evidencia: `db/migraciones-flota/control/0003_modo_como_preset.sql` y 6 pruebas en `db/flota/suite-bd/control.test.mjs`. El mapeo del §3 vive como FILAS en `modo_recorte` y no como un condicional en el código —tarifas, liquidación por cliente, portal del contratante y facturación para `mi_flota`; para `daas` cero filas, y la ausencia ES el mapeo, verificado—, y el recorte se aplica en la RESOLUCIÓN, no borrando `plan_features` ni apagando overrides: si se implementara borrando, conmutar dos veces perdería la configuración del tenant, que es exactamente el caso que el centinela 11 existe para atrapar. DECISIÓN DECLARADA: el recorte del modo gana sobre un override ON, porque el §3 dice «OFF y ocultos» sin matices — si un override pudiera encender tarifas en un tenant `mi_flota`, el modo dejaría de significar algo y la pantalla del contratante aparecería a medias; encenderlas se hace conmutando el modo, y como el recorte no borra nada, conmutar las devuelve intactas (probado: el override sigue ahí y vuelve a mandar en `daas`). El centinela 11 se ejerce comparando, antes y después de `mi_flota→daas→mi_flota`, los conteos de las cinco tablas de entitlements Y la resolución completa de la vista: ni una fila menos, ni un valor distinto. La vista dice además CUÁL feature apagó el modo (`recortada_por_modo`), para que la pantalla «Funciones» del hito g no tenga que adivinarlo. De paso, el guard de lista literal de AC-FTEN-04 hizo su trabajo: la tabla nueva puso el gate en rojo hasta que se la declaró — oráculo: CI [AC-FTEN-22]
- [x] (P2) La opción de INSTANCIA dedicada del plan Empresa queda DOCUMENTADA, no construida (§0 fila Tenancy, §4.1): existe `docs/instancia-dedicada.md` (o sección del runbook enlazada desde ahí) describiendo misma `tenant_template`, otro host, y su condición de NO construida en MVP; test CI grep-able de existencia y contenido mínimo. Evidencia: `docs/instancia-dedicada.md` y su fila en `db/flota/gate-documentos.mjs`, con 5 mutantes propios. El gate exige las tres cosas que el AC nombra —misma plantilla, otro host, y que NO se construye en E1— y el quinto mutante es el que importa: un documento al que se le saca la sección «no construida» y se le pone «disponible ahora» NO pasa, porque sin esa línea el archivo se lee como una promesa de producto y el §0 lo pone en la columna «documentado, no construido» justamente para que nadie lo venda. El documento deja anotado además POR QUÉ la opción es barata (con una base por tenant ya decidida, mover una a otro host es un cambio de operación y no de arquitectura: no hay datos ajenos que separar ni consultas que reescribir) y QUÉ habría que construir cuando entre a un hito —origen de conexión en `control.tenants` sin romper el CHECK `bd = 't_' || slug`, el runner y el centinela 13 alcanzando el host dedicado, el exportador con su prohibición de cross-database intacta, y el pooler propio—, para que ese día no se descubra desde cero. Queda registrada la misma verificación pendiente que el cluster compartido (P2): PostgreSQL ≥ 18 y `CREATE DATABASE … TEMPLATE` a demanda — oráculo: CI [AC-FTEN-23]
- [x] (P1) DDL transversal de hechos del §4.6 presente en `tenant_template` al cierre del hito (a) — los hitos b–d escriben en él antes de que exista el motor de sync (hito e): `eventos`(UUIDv7, tipo catalogado, objeto, actor, dispositivo, event_time+tz_offset, record_time, payload, client_uuid, event_hash — la columna existe; el MOMENTO de su cómputo es la Pregunta 5 de la spec 04 —; `prev_hash` NO se crea en E1, es E3 §4.6) con secuencia monotónica POR TENANT como orden autoritativo, `evidence` (enum completo {firma, foto, lectura, indicador_visual, archivo_logger, documento, pin_destinatario, escaneo_codigo}, `sha256 NOT NULL` en binarios, write-once), `audit_trail` por trigger con retención ≥ la del registro, `client_metric` (enum CERRADO {toques_flujo, eviccion_idb, persist_denegado, outbox_profundidad, outbox_edad_max, pwa_version, sync_error, latencia_ms}, `client_uuid UNIQUE`) y `review_queue`(origen, severidad, asignado_a, sla, estado nueva→reconocida→resuelta con nota); todas con `tenant_id` + CHECK contra `tenant_info`, FK compuesta, índice y `COMMENT ON TABLE` de clase, y append-only donde §7.4 lo exige; casos de rebote: UPDATE/DELETE sobre `eventos`, `evidence` o `audit_trail` como `app_t_<slug>` ⇒ 42501 (§9.3.6) y migración de fixture sin COMMENT de clase ⇒ gate rojo. La CONDUCTA (2xx + flag + fila, ingesta en lote de `client_metric`, escritura de `evidence` del POD, transiciones de `review_queue`) NO nace aquí: módulos 04 y 05 (hito e). Evidencia: `db/migraciones-flota/tenant/0002_hechos_evidencia_y_revision.sql`, con el linter de AC-FTEN-06 en verde sobre sus 6 tablas de dominio y 9 pruebas contra el cluster real en `db/flota/suite-bd/hechos.test.mjs`. El append-only del §7.4 va por DOS caminos, porque cada uno tapa lo que el otro deja pasar: el REVOKE de UPDATE/DELETE al rol `app_t_<slug>` —que `db/flota/rol-app.mjs` DERIVA del catálogo preguntando qué tablas llevan el trigger, en vez de una lista a mano que se desincronizaría con la próxima tabla de hechos— y el trigger `rechazar_mutacion_de_hecho()`, que detiene también a quien tiene más privilegios (un mantenimiento con el rol dueño pasaría por encima del REVOKE); los dos responden 42501, así que el rebote se ve igual venga de donde venga, y TRUNCATE está cubierto con un trigger de sentencia. Append-only: `eventos`, `evidence`, `audit_trail` y `client_metric` (el §4.6 declara append-only también a `client_metric`); `review_queue` no, porque su ciclo de vida ES una transición. `audit_trail` nace ENGANCHADO a `review_queue` —la única tabla mutable de este archivo— para que el mecanismo del trigger exista ejercido y no como código que nadie corrió. DECISIONES DECLARADAS: (a) `eventos.tipo` va contra un catálogo `evento_tipo` y no contra un enum, porque el §4.6 lo llama «catalogado» —a diferencia de `evidence.tipo` y `client_metric.tipo`, donde el maestro sí enumera— y así cada hito agrega los suyos con INSERT, sin migración (§4.9); (b) la secuencia monotónica POR TENANT es una secuencia de Postgres, que en una BD por tenant ya es exactamente eso; (c) `review_queue.severidad` queda como texto a propósito: la ESCALA la fija el módulo 04 (spec 05) y ponerle una acá sería inventar una decisión ajena; (d) el sha256 obligatorio se ata a que HAYA archivo (`archivo_url is null or sha256 is not null`) y no a una lista de tipos «binarios» que el primer vertical nuevo desactualizaría, más un CHECK de 32 bytes; (e) `prev_hash` no se crea (E3) y `event_hash` existe como columna sin computar (Pregunta 5 de la spec 04). El rebote «migración de fixture sin COMMENT de clase ⇒ gate rojo» ya está cubierto por los mutantes del linter en AC-FTEN-06. Hallazgo del camino: el rol de app no tenía USAGE sobre secuencias y la app no podía insertar un hecho —el `nextval` del DEFAULT rebotaba con 42501—; se corrigió en `rol-app.mjs` junto con los privilegios por omisión de secuencias, y la prueba del migrador aserta que la tabla NO esté vacía, porque un UPDATE de cero filas no dispara un trigger FOR EACH ROW y el rebote se leería como verde — oráculo: CI [AC-FTEN-24]
- [x] (P1) Runbook de brechas (§7.8, obligación E1): existe `docs/runbook-brechas.md` versionado en el repo con secciones mínimas — detección, contención, evaluación de alcance POR TENANT (la separación física del §4.1 acota el radio), preservación de evidencia y registro inmutable del incidente, comunicación al tenant afectado, y responsable nombrado — y un test CI grep-able que verifica existencia y la presencia de cada sección (mismo patrón que AC-FTEN-23); el runbook remite a la segregación por tenant de logs, colas y backups de AC-FTEN-16 y al enmascarado de RUT/PIN en logs (§7.8). Evidencia: `docs/runbook-brechas.md` y `db/flota/gate-documentos.mjs`, que corre en `db/flota/gate.sh` (sin `--full`: es estático y barato). El gate no verifica solo que el archivo exista: exige las SEIS secciones mínimas como encabezados —responsable, detección, contención, evaluación de alcance por tenant, preservación de evidencia y registro inmutable, comunicación al tenant— y además SIETE cosas que el runbook tiene que DECIR, que es la diferencia entre un runbook y un archivo vacío con el nombre correcto. 12 mutantes en `db/flota/gate-documentos.test.mjs`: uno por sección, generados desde la misma tabla que el gate usa para que agregar una sección al contrato agregue su mutante solo; más el archivo ausente, el archivo vacío con el nombre correcto, y los tres que sacan el plazo, el responsable y la remisión a AC-FTEN-16. El runbook está escrito para seguirse en orden y cada sección termina en algo que alguien hace: contener antes de entender, no apagar nada que registre —los logs y `audit_trail` son evidencia y detener la escritura «para que no se llene» es destruir la prueba mientras se contiene—, evaluar el alcance tenant por tenant usando que una credencial `app_t_<slug>` alcanza UNA base, y dejar dicho que un tenant que no está en la lista de afectados es uno al que se VERIFICÓ que no le tocó, no uno que no se miró. Registrado también que encontrar una consulta cross-database ES parte del incidente (§7.2) y que un RUT completo o un PIN en un log es un hallazgo más, no un detalle. Los PLAZOS, CANALES y el responsable los fijó el dueño el 09-ago-2026 (Pregunta 10) y son parte exigible del runbook: aviso al `admin_tenant` registrado dentro de **72 h de CONFIRMADA** la brecha, por **correo Y aviso persistente en el panel** hasta que lo reconozca, con **Alexis** como responsable nombrado. Las 72 h son el estándar prudente mientras el reglamento de la Ley 21.719 (vigencia plena 01-dic-2026) no fije otra cosa, y el runbook se escribe para poder ajustar el número sin reescribirse; el test grep-able verifica que las tres cosas —plazo, canales y responsable— estén escritas — oráculo: CI [AC-FTEN-25]
- [ ] (P1) Generador de la suite HTTP A-contra-B AUTOGENERADA del manifiesto de rutas (§9.2, centinela 2 §9.3): un script versionado lee el manifiesto de rutas y EMITE, por CADA ruta, el caso «sesión del tenant A con IDs de B» ⇒ 404 (jamás 403) con body sin cadenas centinela de B, y en mutaciones la BD de B sin cambios; corre dentro de `check.sh --full`; cobertura TOTAL, con exenciones justificadas POR ESCRITO en un archivo versionado y CONTADAS, cuyo contador se emite como artefacto del pipeline con tendencia (§10: tendencia creciente = bandera roja); casos de rebote: agregar una ruta al manifiesto sin caso generado ni exención escrita ⇒ gate rojo; exención sin justificación escrita ⇒ gate rojo; ruta que responde 403 en vez de 404 ante recurso ajeno ⇒ rojo (§0 HTTP). Ningún módulo 01–08 construye suite propia: la declaran en Dependencias apuntando aquí — oráculo: CI [AC-FTEN-26]
- [ ] (P1) Visibilidad por grupos jerárquicos, MECANISMO ejecutable (§3.E1.1 ítem 1 de la lista cerrada E1; §4.4 «árbol ÚNICO de visibilidad ortogonal al rol»). REESCRITO el 09-ago-2026 con la respuesta del dueño a la Pregunta 12, que cerró las cuatro partes que faltaban y que este AC ahora fija: **(a) qué se adscribe a un grupo:** vehículos y usuarios, nada más; **(b) qué superficies filtran:** el tablero «Hoy», la bandeja de encargos y el inventario de vehículos; **(c) cómo compone con el rol:** por INTERSECCIÓN — el rol define QUÉ acciones, el grupo define QUÉ filas; **(d) pertenencia y herencia:** un usuario pertenece a UN grupo y ve su nodo Y todos sus descendientes. Este módulo entrega el mecanismo, no las pantallas: la adscripción (`grupo_id` en las entidades adscribibles), la función de alcance que resuelve «mi nodo y sus descendientes» sobre el árbol de AC-FTEN-12, y una política de visibilidad aplicable a una tabla con UNA línea —mismo patrón que `aplicar_rls_de_dinero()` de AC-FTEN-21—, con `app.current_grupo` viajando por `set_config(..., true)` SET LOCAL por transacción igual que `app.current_role` (§4.1, §7.2). Oráculo doble, como la propia spec exigía: (1) pgTAP de la política con el rol de app REAL sobre una tabla FIXTURE de entidades adscritas —usuario del grupo X ⇒ 0 filas de entidades del grupo Y que no sea su ancestro, y SÍ ve las de sus descendientes—, que FALLA si no ejercita ≥1 tabla (verde vacuo prohibido); (2) e2e de la superficie afectada, que vive en el módulo dueño de cada superficie porque ninguna de las tres existe todavía en el hito (a): tablero «Hoy» y bandeja de encargos en los hitos d/e, inventario de vehículos en el hito c, cada uno declarándolo en Dependencias contra este AC. Casos de rebote: una entidad sin grupo NO se vuelve invisible para todos (la falla no puede ser perder datos de vista) y el rol NO amplía el alcance del grupo ni el grupo amplía el del rol — la composición es intersección y se prueba en los dos sentidos — oráculo: CI [AC-FTEN-27]
- [x] (P1) `db/flota/guardrail.sh` existe versionado en el repo y se ejecuta antes de cada iteración —primer paso de `db/flota/gate.sh`, que a su vez corre dentro de `check.sh --app=flota` antes de todo paso de código— con las TRES reglas del §7.1, cada una ejercida contra el caso que atrapa: (1) `DATABASE_URL` con host distinto de localhost en desarrollo ⇒ exit ≠ 0; (2) secreto fuera de `.env.local`, o `.env.local` no cubierto por `.gitignore` ⇒ exit ≠ 0; (3) grep bloqueante de `TODO|FIXME|PLACEHOLDER|not implemented|lorem ipsum` en `src/` ⇒ exit ≠ 0; casos de rebote: un fixture por CADA una de las tres reglas (DATABASE_URL con host remoto, secreto en archivo versionado, `TODO` sembrado en `src/`) hace fallar el script con exit ≠ 0, y el árbol limpio sale 0. Evidencia: los 9 mutantes de `db/flota/guardrail.test.mjs` en verde — un fixture por regla, más el positivo de la regla 1 (127.0.0.1 pasa: el guard no es un no-op al revés), el negativo del bug real del 06-ago-2026 («TODOS» en español no dispara) y la frontera con KiloPan (una cáscara en `apps/kilopan/src` no le suma un rojo ajeno a FLOTA). El §7.1 de FLOTA NO admite la puerta de «remota intencional» que sí tiene KiloPan, y hay un mutante que lo fija — oráculo: CI [AC-FTEN-28]

## Dependencias

Este módulo es la raíz: ningún módulo 01–08 es prerequisito de su DDL. Sus precondiciones son de PROCESO (§9.1): hito 0 (esqueleto + `packages/metodo` + `packages/miga` + `scripts/deploy.sh`), gate ejecutable «KiloPan DONE» y el hito de extracción de núcleos (`packages/nucleo-*`). Declara, eso sí, qué módulos CIERRAN verificaciones que aquí solo quedan en DDL/patrón:

- **01 · Identidad/enrolamiento (hito b):** crea `personas/usuarios/dispositivos`; los pgTAP de roles de este módulo usan `app.current_role` con el rol de app real, y los flujos de grants de soporte cuyo REGISTRO vive en `control` (§4.1) se implementan allá (§4.3).
- **02 · Vehículos/agenda/EV (hito c):** `turnos` consume `config_version_id` (cierra la congelación por turno de AC-FTEN-13); `vehicle_certification` completa su FK a `vehiculos`; `reading` entra en uso operativo; crea `energy_entry`(type fuel|charge, §4.6 — tercera tabla genérica de aterrizaje del §4.9) junto al uso operativo de recargas, el trigger que clampa la proyección `vehiculos.soc` (CHECK 0–100 SOLO ahí, §0 fila SOC) y los costos de energía que instancian la política de AC-FTEN-21; y CREA la vista `eevd_semanal` (§2 «nace junto al primer módulo operativo»; AC-FVEH-20), que alimenta el campo EEVD agregada del exportador de AC-FTEN-04/AC-FTEN-20 — este módulo la consume, jamás la crea.
- **03 · Encargos/rutas/custodia (hito d):** `encargos.attrs` usa el trigger de `attribute_definition`; `stop_requirement` completa su FK a `paradas`; `cargo_type` queda referenciado; empresa_cliente implícita del modo mi_flota (trigger) al crear `empresas_cliente`.
- **04/05 · POD offline-sync y semáforo (hito e):** CONDUCTA sobre el DDL del §4.6 que este módulo crea (AC-FTEN-24), jamás su creación: motor de sync que honra la clasificación CAPTURA (2xx + flag + review_queue, §4.2) y ejercita `client_uuid` end-to-end (centinela 1); el numerador de la vista `eevd_semanal` (paradas de entrega con evidencia) se completa aquí y el backlog de sync completa ahí su fuente; centinela 10 completo (recarga offline del chofer).
- **06/07 · Tarifas/liquidación y portal del contratante (hito f):** tablas de montos donde se instancia la política RLS de dinero (patrón de AC-FTEN-21); fuente única de devengo SECURITY DEFINER (§7.5); política del rol `cliente` por `empresa_cliente_id`.
- **08 · Panel admin/wizard/seeds (hito g):** el wizard invoca la provisión de AC-FTEN-02; la pantalla «Funciones» opera los entitlements de AC-FTEN-11 (§5.5); los seeds A/B/C consumen `vertical_template` y los centinelas por tenant.

## Preguntas al dueño

**Ninguna queda abierta.** Alexis respondió las once el 09-ago-2026 (registro del acto en
`docs/respuestas-dueno-2026-08-09.md`) y las dos del 08-ago ya estaban cerradas. Quedan
tachadas acá con su respuesta y su razón, como corresponde: la pregunta no se borra, porque
el porqué de una decisión vale tanto como la decisión.

1. ~~**Dominio wildcard de producción**~~ — **RESPONDIDA por Alexis el 09-ago-2026: PARAMETRIZADO, se decide al desplegar.** El dominio entra por variable de entorno; ni el código ni las specs lo cablean, y los tests usan un dominio de fixture. El codename sigue provisional (decisión #4 del maestro) y el valor real se fija recién cuando haya que comprarlo. No desbloquea nada porque nada estaba bloqueado: cierra el hueco sin comprometer un nombre.
2. ~~**Proveedor Postgres gestionado y PgBouncer**~~ — **RESPONDIDA por Alexis el 09-ago-2026: Railway, con PgBouncer en el mismo proyecto.** Coherente con el `railway up` del §9.1 y con el despliegue de KiloPan: una cuenta, una factura. **CONDICIÓN DURA pendiente de verificar, registrada como ÍTEM y no como supuesto:** el §0 exige `uuidv7()` nativo (PostgreSQL ≥ 18) y el §4.1 exige `CREATE DATABASE … TEMPLATE` a demanda. Si el Postgres gestionado de Railway no da las dos cosas, el §4.1 no se puede implementar ahí y la decisión vuelve al dueño. E1 no despliega, así que hay tiempo — pero se verifica, no se supone.
3. ~~**¿Las 3 tablas de entitlements en `control` o en la BD tenant?**~~ — **RESPONDIDA por Alexis el 09-ago-2026: en `control`.** El ruteo ya lee `control` para resolver el subdominio (§4.1), así que el bootstrap no abre un camino cross-database nuevo: viaja por el mismo. El entitlement efectivo se resuelve ahí y se CONGELA en el snapshot de config del tenant (ver Pregunta 4), con lo cual el runtime del producto nunca vuelve a consultar `control`. Confirma lo que esta spec ya asumía; desbloquea AC-FTEN-11.
4. ~~**Granularidad de `config_version_id`**~~ — **RESPONDIDA por Alexis el 09-ago-2026: snapshot GLOBAL por tenant.** Una fila de `config_version` por cada cambio, que congela TODA la configuración del tenant: tema, terminología, entitlements efectivos, parámetros y plantilla de vertical. `turno.config_version_id` apunta ahí. Es lo único que hace literalmente cierto el «un turno corre entero con UNA versión» del §4.4, y el drill-down desde un turno viejo pasa a ser una sola lectura en vez de reconstruir un snapshot desde N punteros. Desbloquea AC-FTEN-13 completo.
5. ~~**Claves de `parametros` para E1**~~ — **RESPONDIDA por Alexis el 09-ago-2026: lista CERRADA de 8 claves, DICTADA.** `reserva_pct` 15 · `factor_consumo` 0,85 (las dos las fija el §0, no el dueño) · `tarifa_kwh_clp` 190 · `precio_diesel_litro_clp` 1260 · `bultos_max_sin_receptor` 0 · `anticipacion_vencimiento_dias` 30 · `tolerancia_eta_minutos` 20 · `periodicidad_liquidacion` `'semanal'`. Detalle y razones en `docs/respuestas-dueno-2026-08-09.md` §P5. **Tres consecuencias que se aplican, no solo se siembran:** (1) `bultos_max_sin_receptor = 0` significa «cuántos bultos se dejan SIN encuadre», o sea que TODO `dejado_en_punto` exige foto — el §5.2 F4 pasa de «3 acciones con encuadre si supera el umbral» a **4 acciones fijas**, que sigue bajo el techo de 4 del §5.3 pero sin margen: el e2e de 04 · AC-FPOD-02 tiene que asertar 4; (2) `periodicidad_liquidacion` es por TENANT y no por empresa cliente, lo que cierra la pregunta 1 de la spec 06; (3) la tolerancia dictada es 20 y no el mínimo de 15 del Anexo B: el mínimo vive como CHECK de la columna y el valor, en el seed. Los VALORES son seeds del hito (g); las COLUMNAS y sus invariantes ya están en `db/migraciones-flota/tenant/0005_parametros_lista_cerrada.sql` (AC-FTEN-12).
6. ~~**Matriz feature×plan**~~ — **RESPONDIDA por Alexis el 09-ago-2026: la deriva el builder, la revisa el dueño con los seeds.** Se arma con todos los `lookup_key` que las 9 specs nombran, repartidos por plan según las pistas del Anexo A (Partida: 1 vehículo, 300 entregas/mes · Pro: white-label completo + API + frío básico · Empresa: SSO, compliance, ERP), **con la justificación de cada asignación escrita en la spec**. Alexis la revisa en el hito (g), cuando los seeds A/B/C la vuelvan concreta y pueda ver qué ve cada plan en pantalla.
7. ~~**Contraste del acento**~~ — **RESPONDIDA por Alexis el 09-ago-2026: cada variante contra SU fondo.** La plataforma deriva una variante del acento por tema y valida cada una contra el fondo de ESE tema: la clara contra el fondo claro, la oscura contra el fondo oscuro, ambas ≥ 4,5:1. **El dueño rechazó explícitamente la propuesta que esta misma spec sugería** («rechazar si falla contra cualquiera de los dos fondos») por inservible: el producto del ratio de un color contra blanco por su ratio contra negro es exactamente 21, así que exigir ≥4,5:1 contra ambos deja pasar solo acentos con ratio entre 4,50 y 4,67 contra blanco — una rendija donde casi ningún color de marca entra. La opción elegida siempre se puede satisfacer aclarando u oscureciendo la variante, que es justo lo que el §5.1 dice que la plataforma hace. Desbloquea AC-FTEN-10 completo, incluido su fixture de rechazo (un acento que, derivado para el tema oscuro, no llega a 4,5:1 contra el fondo oscuro). *Pregunta CANÓNICA del par de contraste del acento: la spec 08 (pregunta 08-6) hereda esta respuesta.*
8. ~~**Cadencia del job exportador a `control`**~~ — **RESPONDIDA por Alexis el 09-ago-2026: cada 5 minutos.** Hace que los «2 intervalos» del Anexo B sean 10 minutos, coherente con el otro umbral de la misma tabla («errores >5% por 15 min»). Son agregados pequeños por tenant: el costo es despreciable y el panel cross-tenant se siente vivo sin ser tiempo real. Desbloquea AC-FTEN-20 y, heredada, la pregunta 3 de la spec 05. *Pregunta CANÓNICA de la cadencia del exportador.*
9. ~~**Semántica de fallo del ruteo**~~ — **RESPONDIDA por Alexis el 09-ago-2026: 404 · 503 · 404.** `control.tenants.estado ∈ {activo, suspendido, archivado}`. Subdominio SIN tenant en `control` ⇒ **404**, para no revelar si el subdominio existe (mismo criterio que el recurso ajeno del §0). Tenant `suspendido` ⇒ **503** con página propia en es-CL y CERO acceso a su base. Tenant `archivado` ⇒ **404**, como si no existiera. Desbloquea el caso de rebote de AC-FTEN-05.
10. ~~**Runbook de brechas — plazos, canales y responsable**~~ — **RESPONDIDA por Alexis el 09-ago-2026: 72 h · correo + panel · responsable Alexis.** Aviso al `admin_tenant` registrado dentro de 72 h de CONFIRMADA la brecha, por correo y con aviso persistente en el panel hasta que lo reconozca. Responsable nombrado: **Alexis**. Las 72 h son el estándar prudente mientras el reglamento de la Ley 21.719 (vigencia plena 01-dic-2026) no fije otra cosa; el runbook queda escrito para poder ajustar el número sin reescribirse. Desbloquea la sección que faltaba de AC-FTEN-25.
11. ~~**Clase (PLANIFICACIÓN|CAPTURA) del `COMMENT ON TABLE` de `lot`**~~ — **RESPONDIDA por Alexis el 08-ago-2026: CAPTURA.** Razón registrada: un número de lote se lee de la caja física, en terreno y posiblemente sin señal; con PLANIFICACIÓN ese registro rebotaría 422 y se perdería, que es justo lo que el §4.2 prohíbe. Desbloquea la cláusula de `lot` en AC-FTEN-14. *Pregunta CANÓNICA de la clase del gancho `lot`: la spec 03 (pregunta 03-8) hereda esta respuesta.*
12. ~~**Visibilidad por grupos jerárquicos**~~ — **RESPONDIDA por Alexis el 09-ago-2026: vehículos + usuarios · 3 superficies · intersección con el rol · un grupo con herencia hacia abajo.** Las cuatro partes: **(a)** se adscriben a un grupo **vehículos y usuarios**, nada más; **(b)** filtran por grupo el tablero «Hoy», la bandeja de encargos y el inventario de vehículos; **(c)** compone con el rol por **intersección** — el rol define QUÉ acciones, el grupo define QUÉ filas, ortogonales como dice el §4.4; **(d)** un usuario pertenece a **UN** grupo y ve su nodo **y todos sus descendientes**. Es el patrón de tags jerárquicos de Samsara y groups+clearances de Geotab que el §6 ya cita como referente. **Desbloquea AC-FTEN-27 entero** — era el único AC de esta spec bloqueado de punta a punta.
13. ~~**Clase (PLANIFICACIÓN|CAPTURA) del `COMMENT ON TABLE` de `reference_document`**~~ — **RESPONDIDA por Alexis el 08-ago-2026: CAPTURA.** Dirime la contradicción entre la spec 03 (CAPTURA) y la 06 (PLANIFICACIÓN) a favor de la 03. Razón registrada: el DTE se asocia en la parada de carga antes de que el camión se vaya (§7.3, art. 55 DL 825); si el registro rebotara sin señal, el camión no sale. Que la 06 registre folios de liquidación estando online no la incomoda: una tabla CAPTURA también acepta escrituras con red — lo que la clase garantiza es que nunca rebota, no que nunca haya conexión. Desbloquea la cláusula de `reference_document` en AC-FTEN-14. *Pregunta CANÓNICA de la clase de `reference_document`: las specs 03 y 06 heredan esta respuesta.*
